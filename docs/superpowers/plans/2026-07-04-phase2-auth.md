# Phase 2: 認証 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `apps/server` のファイル操作 API を httpOnly Cookie + JWT で保護し、`apps/web` にログイン画面と 401 セッション切れ処理を追加する。

**Architecture:** ログイン成功で署名付き JWT（`hono/jwt`）を httpOnly Cookie にセット。`createApp` が `/api/auth`（公開）をマウントし、`/api/*` に「`/api/auth/*` を除外して JWT を検証する」ガードミドルウェアを掛けてから files ルートをマウントする（Hono のミドルウェアは登録順に後続ルートへ適用されるため、auth→ガード→files の順で重なりを避ける）。パスワードは Node 標準 scrypt でハッシュ化し環境変数で保持。設計は `docs/superpowers/specs/2026-07-04-phase2-auth-design.md`。

**Tech Stack:** Hono + `hono/jwt` + `hono/cookie` / Node `crypto`(scrypt) / Vitest / React 19 + TanStack Query / shadcn/ui

## Global Constraints

- **依存追加なし**: 認証は `hono/jwt`・`hono/cookie`（既存 `hono` に同梱）と `node:crypto` のみ。新規 npm 依存を追加しない
- **禁止コマンド**: `curl` / `wget` / `rm -rf` / `env` / `printenv` / `git push --force`。HTTP 疎通確認は Node の `fetch`。`.env*` は読まない
- **TypeScript**: `erasableSyntaxOnly` 有効 → parameter property・enum 禁止（クラスはフィールド明示代入）。`verbatimModuleSyntax` 有効 → 型のみ import/export は `import type`/`export type`。`baseUrl` は使わない・`paths` の値は相対
- **import 規約**: server 内部は相対 import、`@nas-fm/shared` は型のみ。web は feature 間 import を各 feature の `index.ts` 経由、UI プリミティブは `@/components/ui/*`
- **テスト**: server は実挙動を Hono `app.request()` で検証（HTTP サーバ起動なし）。web は Vitest(jsdom) + Testing Library、API は `@/lib/api` をモック、TanStack Query は `retry:false` でラップ。Vitest imports は明示
- **命名/秘密情報**: Cookie 名 `nasfm_token`。環境変数 `AUTH_SECRET` / `AUTH_PASSWORD_HASH`。ハッシュ形式 `scrypt$<saltBase64>$<hashBase64>`。開発デフォルトのパスワードは `admin`、開発 secret は `dev-insecure-secret-change-me`
- **コミット**: Conventional Commits（接頭辞英語・本文日本語）。pre-commit で lint-staged（oxfmt → oxlint --fix → typecheck）が自動実行。1タスク=1コミット。Node 24.16.0 固定

---

## File Structure

```
packages/shared/src/types.ts        # T1: ApiErrorCode に UNAUTHORIZED、LoginRequest / AuthStatus 追加
packages/shared/src/index.ts        # T1: 追加 export
apps/server/src/lib/errors.ts       # T1: statusOf に UNAUTHORIZED→401
apps/server/src/lib/errors.test.ts  # T1: 401 ケース追加
apps/server/src/lib/password.ts       # T2: hashPassword / verifyPassword
apps/server/src/lib/password.test.ts  # T2
apps/server/scripts/hash-password.ts  # T2: 本番ハッシュ生成 CLI
apps/server/src/lib/auth-config.ts       # T3: resolveAuthConfig
apps/server/src/lib/auth-config.test.ts  # T3
apps/server/src/features/auth/auth.service.ts       # T4: verifyLogin / issueToken / verifyToken
apps/server/src/features/auth/auth.service.test.ts  # T4
apps/server/src/features/auth/auth.middleware.ts    # T5: requireAuth, COOKIE_NAME
apps/server/src/features/auth/auth.routes.ts        # T5: createAuthRoutes
apps/server/src/features/auth/auth.routes.test.ts   # T5（新規: login/logout/me/保護ルート）
apps/server/src/app.ts              # T5: createApp(root, authConfig) + ガード
apps/server/src/server.ts           # T5: resolveAuthConfig を配線
apps/server/src/features/files/files.routes.test.ts # T5: 各リクエストに認証 Cookie を付与
apps/web/src/lib/api.ts             # T6: login / logout / me 追加
apps/web/src/lib/error-messages.ts  # T6: UNAUTHORIZED 追加
apps/web/src/lib/*.test.ts          # T6
apps/web/src/features/auth/hooks/useAuth.ts             # T7
apps/web/src/features/auth/components/LoginForm.tsx     # T7
apps/web/src/features/auth/components/LogoutButton.tsx  # T7
apps/web/src/features/auth/components/AuthGate.tsx      # T7
apps/web/src/features/auth/index.ts                     # T7: AuthGate / LogoutButton 公開
apps/web/src/app/providers.tsx      # T8: 401→["me"] 無効化の中央ハンドラ
apps/web/src/app/App.tsx            # T8: AuthGate で FileBrowser を包む + LogoutButton
apps/web/src/app/App.test.tsx       # T8
docs/roadmap.md                     # T9
```

`apps/server/src/features/auth/.gitkeep` と `apps/web/src/features/auth/.gitkeep` は実ファイル追加時に削除する。

---

### Task 1: 共有型と 401 ステータス

**Files:**
- Modify: `packages/shared/src/types.ts`, `packages/shared/src/index.ts`, `apps/server/src/lib/errors.ts`, `apps/server/src/lib/errors.test.ts`

**Interfaces:**
- Produces: `ApiErrorCode` に `"UNAUTHORIZED"` 追加、`LoginRequest { password: string }`、`AuthStatus { authenticated: boolean }`。`statusOf("UNAUTHORIZED") === 401`（戻り型 `400 | 401 | 404 | 409 | 500`）

- [ ] **Step 1: shared に型を追加**

`packages/shared/src/types.ts` の `ApiErrorCode` に `UNAUTHORIZED` を追加（`INTERNAL` の行の前）:
```ts
export type ApiErrorCode =
  | "PATH_TRAVERSAL"
  | "INVALID_REQUEST"
  | "NOT_A_DIRECTORY"
  | "IS_A_DIRECTORY"
  | "NOT_FOUND"
  | "CONFLICT"
  | "UNAUTHORIZED"
  | "INTERNAL";
```
同ファイル末尾に追記:
```ts
export interface LoginRequest {
  password: string;
}

export interface AuthStatus {
  authenticated: boolean;
}
```

`packages/shared/src/index.ts` を全置換:
```ts
export type {
  ApiError,
  ApiErrorCode,
  AuthStatus,
  FileEntry,
  FileType,
  ListResponse,
  LoginRequest,
  MkdirRequest,
  OkResponse,
  RenameRequest,
} from "./types";
```

- [ ] **Step 2: errors.test.ts に 401 ケースを追加**

`apps/server/src/lib/errors.test.ts` の `statusOf` の `it.each` テーブルに1行追加（`["NOT_FOUND", 404]` の前など）:
```ts
    ["UNAUTHORIZED", 401],
```

- [ ] **Step 3: テストが失敗することを確認**

Run: `npm run test -w @nas-fm/server`
Expected: FAIL（`statusOf` が UNAUTHORIZED を未処理 → 型エラーまたはテスト失敗）

- [ ] **Step 4: `statusOf` を更新**

`apps/server/src/lib/errors.ts` の `statusOf` を全置換:
```ts
export function statusOf(code: ApiErrorCode): 400 | 401 | 404 | 409 | 500 {
  switch (code) {
    case "PATH_TRAVERSAL":
    case "INVALID_REQUEST":
    case "NOT_A_DIRECTORY":
    case "IS_A_DIRECTORY":
      return 400;
    case "UNAUTHORIZED":
      return 401;
    case "NOT_FOUND":
      return 404;
    case "CONFLICT":
      return 409;
    case "INTERNAL":
      return 500;
  }
}
```

- [ ] **Step 5: テスト・typecheck を確認してコミット**

```bash
npm run test -w @nas-fm/server
npm run typecheck
git add packages/shared/src/types.ts packages/shared/src/index.ts apps/server/src/lib/errors.ts apps/server/src/lib/errors.test.ts
git commit -m "feat: 認証用の共有型と UNAUTHORIZED(401) を追加"
```
Expected: 全 PASS、typecheck 0（3ワークスペース）。

---

### Task 2: パスワードハッシュ（scrypt）とハッシュ生成 CLI

**Files:**
- Create: `apps/server/src/lib/password.ts` + `password.test.ts`, `apps/server/scripts/hash-password.ts`

**Interfaces:**
- Produces: `hashPassword(password: string): string`（`scrypt$<saltBase64>$<hashBase64>` を返す）、`verifyPassword(password: string, stored: string): boolean`

- [ ] **Step 1: 失敗するテストを書く**

`apps/server/src/lib/password.test.ts`:
```ts
import { describe, expect, it } from "vitest";
import { hashPassword, verifyPassword } from "./password";

describe("password", () => {
  it("ハッシュは scrypt$salt$hash 形式", () => {
    const h = hashPassword("secret");
    expect(h.startsWith("scrypt$")).toBe(true);
    expect(h.split("$")).toHaveLength(3);
  });

  it("同じパスワードでも salt が異なるので毎回違うハッシュ", () => {
    expect(hashPassword("secret")).not.toBe(hashPassword("secret"));
  });

  it("正しいパスワードを検証できる", () => {
    const h = hashPassword("secret");
    expect(verifyPassword("secret", h)).toBe(true);
  });

  it("誤ったパスワードを拒否する", () => {
    const h = hashPassword("secret");
    expect(verifyPassword("wrong", h)).toBe(false);
  });

  it("壊れた保存値は false", () => {
    expect(verifyPassword("secret", "garbage")).toBe(false);
    expect(verifyPassword("secret", "scrypt$only-two")).toBe(false);
    expect(verifyPassword("secret", "bcrypt$a$b")).toBe(false);
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `npm run test -w @nas-fm/server`
Expected: FAIL（`./password` が無い）

- [ ] **Step 3: `apps/server/src/lib/password.ts` を実装**

```ts
import { randomBytes, scryptSync, timingSafeEqual } from "node:crypto";

const KEYLEN = 64;
const SALT_BYTES = 16;

export function hashPassword(password: string): string {
  const salt = randomBytes(SALT_BYTES);
  const hash = scryptSync(password, salt, KEYLEN);
  return `scrypt$${salt.toString("base64")}$${hash.toString("base64")}`;
}

export function verifyPassword(password: string, stored: string): boolean {
  const parts = stored.split("$");
  if (parts.length !== 3 || parts[0] !== "scrypt") return false;
  const salt = Buffer.from(parts[1], "base64");
  const expected = Buffer.from(parts[2], "base64");
  if (expected.length !== KEYLEN) return false;
  const actual = scryptSync(password, salt, KEYLEN);
  return timingSafeEqual(actual, expected);
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `npm run test -w @nas-fm/server`
Expected: PASS。

- [ ] **Step 5: ハッシュ生成 CLI を作成**

`apps/server/scripts/hash-password.ts`:
```ts
import { hashPassword } from "../src/lib/password";

const password = process.argv[2];
if (!password) {
  console.error("Usage: npx tsx apps/server/scripts/hash-password.ts <password>");
  process.exit(1);
}
console.log(hashPassword(password));
```

動作確認（本番の AUTH_PASSWORD_HASH 値を生成する用途）:
```bash
npx tsx apps/server/scripts/hash-password.ts mypassword
```
Expected: `scrypt$...$...` の1行が出力される。

- [ ] **Step 6: コミット**

```bash
git add apps/server/src/lib/password.ts apps/server/src/lib/password.test.ts apps/server/scripts/hash-password.ts
git commit -m "feat: scrypt によるパスワードハッシュとハッシュ生成スクリプトを追加"
```

---

### Task 3: 認証設定の解決（env ＋ 開発デフォルト）

**Files:**
- Create: `apps/server/src/lib/auth-config.ts` + `auth-config.test.ts`

**Interfaces:**
- Consumes: `hashPassword`（`./password`）
- Produces: `interface AuthConfig { secret: string; passwordHash: string }`、`resolveAuthConfig(): AuthConfig`（env があれば使用、無ければ開発デフォルト＋`console.warn`）

- [ ] **Step 1: 失敗するテストを書く**

`apps/server/src/lib/auth-config.test.ts`:
```ts
import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";
import { verifyPassword } from "./password";
import { resolveAuthConfig } from "./auth-config";

let savedSecret: string | undefined;
let savedHash: string | undefined;

beforeEach(() => {
  savedSecret = process.env.AUTH_SECRET;
  savedHash = process.env.AUTH_PASSWORD_HASH;
});

afterEach(() => {
  if (savedSecret === undefined) delete process.env.AUTH_SECRET;
  else process.env.AUTH_SECRET = savedSecret;
  if (savedHash === undefined) delete process.env.AUTH_PASSWORD_HASH;
  else process.env.AUTH_PASSWORD_HASH = savedHash;
  vi.restoreAllMocks();
});

describe("resolveAuthConfig", () => {
  it("env が設定済みならそれを使い警告しない", () => {
    process.env.AUTH_SECRET = "real-secret";
    process.env.AUTH_PASSWORD_HASH = "scrypt$abc$def";
    const warn = vi.spyOn(console, "warn").mockImplementation(() => {});
    const config = resolveAuthConfig();
    expect(config.secret).toBe("real-secret");
    expect(config.passwordHash).toBe("scrypt$abc$def");
    expect(warn).not.toHaveBeenCalled();
  });

  it("env 未設定なら開発デフォルト（admin を検証可能）＋警告", () => {
    delete process.env.AUTH_SECRET;
    delete process.env.AUTH_PASSWORD_HASH;
    const warn = vi.spyOn(console, "warn").mockImplementation(() => {});
    const config = resolveAuthConfig();
    expect(config.secret).toBe("dev-insecure-secret-change-me");
    expect(verifyPassword("admin", config.passwordHash)).toBe(true);
    expect(warn).toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `npm run test -w @nas-fm/server`
Expected: FAIL（`./auth-config` が無い）

- [ ] **Step 3: `apps/server/src/lib/auth-config.ts` を実装**

```ts
import { hashPassword } from "./password";

export interface AuthConfig {
  secret: string;
  passwordHash: string;
}

const DEV_SECRET = "dev-insecure-secret-change-me";
const DEV_PASSWORD = "admin";

/**
 * 認証設定を環境変数から解決する。
 * AUTH_SECRET / AUTH_PASSWORD_HASH が未設定の場合は開発用のデフォルト（パスワード "admin"）に
 * フォールバックし、本番では環境変数を設定するよう警告する。
 */
export function resolveAuthConfig(): AuthConfig {
  const envSecret = process.env.AUTH_SECRET;
  const envHash = process.env.AUTH_PASSWORD_HASH;
  if (!envSecret || !envHash) {
    console.warn(
      "WARNING: 開発用の認証設定を使用中。本番では AUTH_SECRET と AUTH_PASSWORD_HASH を設定すること",
    );
  }
  return {
    secret: envSecret ?? DEV_SECRET,
    passwordHash: envHash ?? hashPassword(DEV_PASSWORD),
  };
}
```

- [ ] **Step 4: テストが通ることを確認してコミット**

```bash
npm run test -w @nas-fm/server
git add apps/server/src/lib/auth-config.ts apps/server/src/lib/auth-config.test.ts
git commit -m "feat: 認証設定の解決（開発デフォルト＋警告）を追加"
```
Expected: PASS。

---

### Task 4: 認証サービス（ログイン照合・JWT 発行/検証）

**Files:**
- Create: `apps/server/src/features/auth/auth.service.ts` + `auth.service.test.ts`

**Interfaces:**
- Consumes: `AuthConfig`（`../../lib/auth-config`）, `verifyPassword`（`../../lib/password`）, `hono/jwt` の `sign`/`verify`
- Produces: `verifyLogin(config: AuthConfig, password: string): boolean`、`issueToken(config: AuthConfig): Promise<string>`、`verifyToken(config: AuthConfig, token: string): Promise<boolean>`、`TOKEN_TTL_SECONDS`（number, 604800）

- [ ] **Step 1: 失敗するテストを書く**

`apps/server/src/features/auth/auth.service.test.ts`:
```ts
import { sign } from "hono/jwt";
import { describe, expect, it } from "vitest";
import type { AuthConfig } from "../../lib/auth-config";
import { hashPassword } from "../../lib/password";
import { issueToken, verifyLogin, verifyToken } from "./auth.service";

const config: AuthConfig = { secret: "test-secret", passwordHash: hashPassword("pw") };

describe("verifyLogin", () => {
  it("正しいパスワードで true", () => {
    expect(verifyLogin(config, "pw")).toBe(true);
  });
  it("誤ったパスワードで false", () => {
    expect(verifyLogin(config, "nope")).toBe(false);
  });
});

describe("issueToken / verifyToken", () => {
  it("発行したトークンを検証できる", async () => {
    const token = await issueToken(config);
    expect(await verifyToken(config, token)).toBe(true);
  });

  it("別の secret で署名されたトークンは拒否", async () => {
    const token = await sign({ sub: "admin", exp: Math.floor(Date.now() / 1000) + 60 }, "other-secret");
    expect(await verifyToken(config, token)).toBe(false);
  });

  it("期限切れトークンは拒否", async () => {
    const token = await sign({ sub: "admin", exp: Math.floor(Date.now() / 1000) - 10 }, config.secret);
    expect(await verifyToken(config, token)).toBe(false);
  });

  it("壊れたトークンは拒否", async () => {
    expect(await verifyToken(config, "not-a-jwt")).toBe(false);
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `npm run test -w @nas-fm/server`
Expected: FAIL（`./auth.service` が無い）

- [ ] **Step 3: `apps/server/src/features/auth/auth.service.ts` を実装**

```ts
import { sign, verify } from "hono/jwt";
import type { AuthConfig } from "../../lib/auth-config";
import { verifyPassword } from "../../lib/password";

export const TOKEN_TTL_SECONDS = 60 * 60 * 24 * 7; // 7 日

export function verifyLogin(config: AuthConfig, password: string): boolean {
  return verifyPassword(password, config.passwordHash);
}

export function issueToken(config: AuthConfig): Promise<string> {
  const exp = Math.floor(Date.now() / 1000) + TOKEN_TTL_SECONDS;
  return sign({ sub: "admin", exp }, config.secret);
}

export async function verifyToken(config: AuthConfig, token: string): Promise<boolean> {
  try {
    await verify(token, config.secret);
    return true;
  } catch {
    return false;
  }
}
```

- [ ] **Step 4: テストが通ることを確認してコミット**

```bash
npm run test -w @nas-fm/server
git add apps/server/src/features/auth/auth.service.ts apps/server/src/features/auth/auth.service.test.ts
git commit -m "feat: 認証サービス（ログイン照合・JWT発行検証）を追加"
```
Expected: PASS。

---

### Task 5: ミドルウェア・ルート・アプリ配線（保護の統合）

**Files:**
- Create: `apps/server/src/features/auth/auth.middleware.ts`, `apps/server/src/features/auth/auth.routes.ts`, `apps/server/src/features/auth/auth.routes.test.ts`
- Modify: `apps/server/src/app.ts`, `apps/server/src/server.ts`, `apps/server/src/features/files/files.routes.test.ts`
- Delete: `apps/server/src/features/auth/.gitkeep`

**Interfaces:**
- Consumes: `AuthConfig`, `verifyLogin`/`issueToken`/`verifyToken`, `AppError`, `hono/cookie`（`setCookie`/`getCookie`/`deleteCookie`）
- Produces: `COOKIE_NAME = "nasfm_token"`、`requireAuth(config: AuthConfig): MiddlewareHandler`、`createAuthRoutes(config: AuthConfig): Hono`、`createApp(root: string, authConfig: AuthConfig): Hono`

- [ ] **Step 1: auth のルート/保護テストを書く（失敗する）**

`apps/server/src/features/auth/auth.routes.test.ts`:
```ts
import { mkdtemp, rm } from "node:fs/promises";
import { tmpdir } from "node:os";
import path from "node:path";
import { afterEach, beforeEach, describe, expect, it } from "vitest";
import { createApp } from "../../app";
import type { AuthConfig } from "../../lib/auth-config";
import { hashPassword } from "../../lib/password";

const config: AuthConfig = { secret: "test-secret", passwordHash: hashPassword("pw") };
let root: string;

beforeEach(async () => {
  root = await mkdtemp(path.join(tmpdir(), "nasfm-auth-"));
});
afterEach(async () => {
  await rm(root, { recursive: true, force: true });
});

const jsonHeaders = { "content-type": "application/json" };

function cookieFrom(res: Response): string {
  const setCookie = res.headers.get("set-cookie") ?? "";
  return setCookie.split(";")[0]; // "nasfm_token=<token>"
}

describe("POST /api/auth/login", () => {
  it("正しいパスワードで Cookie を発行し 200", async () => {
    const app = createApp(root, config);
    const res = await app.request("/api/auth/login", {
      method: "POST",
      headers: jsonHeaders,
      body: JSON.stringify({ password: "pw" }),
    });
    expect(res.status).toBe(200);
    expect(res.headers.get("set-cookie") ?? "").toContain("nasfm_token=");
    expect(res.headers.get("set-cookie") ?? "").toContain("HttpOnly");
  });

  it("誤ったパスワードは 401 UNAUTHORIZED", async () => {
    const app = createApp(root, config);
    const res = await app.request("/api/auth/login", {
      method: "POST",
      headers: jsonHeaders,
      body: JSON.stringify({ password: "wrong" }),
    });
    expect(res.status).toBe(401);
    const body = (await res.json()) as { error: { code: string } };
    expect(body.error.code).toBe("UNAUTHORIZED");
  });

  it("password 欠落は 400", async () => {
    const app = createApp(root, config);
    const res = await app.request("/api/auth/login", {
      method: "POST",
      headers: jsonHeaders,
      body: JSON.stringify({}),
    });
    expect(res.status).toBe(400);
  });
});

describe("GET /api/auth/me", () => {
  it("Cookie 無しは authenticated:false（200）", async () => {
    const app = createApp(root, config);
    const res = await app.request("/api/auth/me");
    expect(res.status).toBe(200);
    expect(await res.json()).toEqual({ authenticated: false });
  });

  it("ログイン後の Cookie で authenticated:true", async () => {
    const app = createApp(root, config);
    const login = await app.request("/api/auth/login", {
      method: "POST",
      headers: jsonHeaders,
      body: JSON.stringify({ password: "pw" }),
    });
    const res = await app.request("/api/auth/me", { headers: { Cookie: cookieFrom(login) } });
    expect(await res.json()).toEqual({ authenticated: true });
  });
});

describe("POST /api/auth/logout", () => {
  it("Cookie を失効させる", async () => {
    const app = createApp(root, config);
    const res = await app.request("/api/auth/logout", { method: "POST" });
    expect(res.status).toBe(200);
    expect(res.headers.get("set-cookie") ?? "").toContain("nasfm_token=");
    // 失効は Max-Age=0 もしくは過去日で表現される
    expect(res.headers.get("set-cookie") ?? "").toMatch(/Max-Age=0|Expires=/);
  });
});

describe("保護されたファイルルート", () => {
  it("Cookie 無しの /api/list は 401", async () => {
    const app = createApp(root, config);
    const res = await app.request("/api/list?path=");
    expect(res.status).toBe(401);
  });

  it("有効な Cookie の /api/list は 200", async () => {
    const app = createApp(root, config);
    const login = await app.request("/api/auth/login", {
      method: "POST",
      headers: jsonHeaders,
      body: JSON.stringify({ password: "pw" }),
    });
    const res = await app.request("/api/list?path=", { headers: { Cookie: cookieFrom(login) } });
    expect(res.status).toBe(200);
  });

  it("/health は認証なしで 200", async () => {
    const app = createApp(root, config);
    const res = await app.request("/health");
    expect(res.status).toBe(200);
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `npm run test -w @nas-fm/server`
Expected: FAIL（`createApp` が引数2つを受けない / auth ルートが無い）

- [ ] **Step 3: `auth.middleware.ts` を実装**

```ts
import type { MiddlewareHandler } from "hono";
import { getCookie } from "hono/cookie";
import type { AuthConfig } from "../../lib/auth-config";
import { AppError } from "../../lib/errors";
import { verifyToken } from "./auth.service";

export const COOKIE_NAME = "nasfm_token";

export function requireAuth(config: AuthConfig): MiddlewareHandler {
  return async (c, next) => {
    const token = getCookie(c, COOKIE_NAME);
    if (!token || !(await verifyToken(config, token))) {
      throw new AppError("UNAUTHORIZED", "authentication required");
    }
    await next();
  };
}
```

- [ ] **Step 4: `auth.routes.ts` を実装**

```ts
import { Hono } from "hono";
import { deleteCookie, getCookie, setCookie } from "hono/cookie";
import type { AuthStatus, OkResponse } from "@nas-fm/shared";
import type { AuthConfig } from "../../lib/auth-config";
import { AppError } from "../../lib/errors";
import { COOKIE_NAME } from "./auth.middleware";
import { TOKEN_TTL_SECONDS, issueToken, verifyLogin, verifyToken } from "./auth.service";

async function readPassword(readJson: () => Promise<unknown>): Promise<string> {
  let body: unknown;
  try {
    body = await readJson();
  } catch {
    throw new AppError("INVALID_REQUEST", "invalid JSON body");
  }
  if (typeof body !== "object" || body === null || typeof (body as { password?: unknown }).password !== "string") {
    throw new AppError("INVALID_REQUEST", "password is required");
  }
  return (body as { password: string }).password;
}

export function createAuthRoutes(config: AuthConfig): Hono {
  const app = new Hono();

  app.post("/login", async (c) => {
    const password = await readPassword(() => c.req.json());
    if (!verifyLogin(config, password)) {
      throw new AppError("UNAUTHORIZED", "invalid password");
    }
    const token = await issueToken(config);
    setCookie(c, COOKIE_NAME, token, {
      httpOnly: true,
      sameSite: "Lax",
      path: "/",
      maxAge: TOKEN_TTL_SECONDS,
    });
    const res: OkResponse = { ok: true };
    return c.json(res);
  });

  app.post("/logout", (c) => {
    deleteCookie(c, COOKIE_NAME, { path: "/" });
    const res: OkResponse = { ok: true };
    return c.json(res);
  });

  app.get("/me", async (c) => {
    const token = getCookie(c, COOKIE_NAME);
    const authenticated = token ? await verifyToken(config, token) : false;
    const res: AuthStatus = { authenticated };
    return c.json(res);
  });

  return app;
}
```

- [ ] **Step 5: `app.ts` を全置換（createApp(root, authConfig) + ガード）**

```ts
import { Hono } from "hono";
import type { ApiError } from "@nas-fm/shared";
import type { AuthConfig } from "./lib/auth-config";
import { AppError, statusOf } from "./lib/errors";
import { createAuthRoutes } from "./features/auth/auth.routes";
import { requireAuth } from "./features/auth/auth.middleware";
import { createFilesRoutes } from "./features/files/files.routes";

export function createApp(root: string, authConfig: AuthConfig): Hono {
  const app = new Hono();

  app.get("/health", (c) => c.json({ status: "ok" }));

  // 認証ルート（login は公開）。files のガードより先に登録する。
  app.route("/api/auth", createAuthRoutes(authConfig));

  // /api/auth/* を除いた /api/* を JWT で保護する。
  // （files を /api にマウントすると /api/auth と接頭辞が重なるため、ここで明示的に除外する）
  const guard = requireAuth(authConfig);
  app.use("/api/*", async (c, next) => {
    if (c.req.path.startsWith("/api/auth/")) {
      return next();
    }
    return guard(c, next);
  });

  app.route("/api", createFilesRoutes(root));

  app.onError((err, c) => {
    if (err instanceof AppError && err.code !== "INTERNAL") {
      const body: ApiError = { error: { code: err.code, message: err.message } };
      return c.json(body, statusOf(err.code));
    }
    console.error(err);
    const body: ApiError = { error: { code: "INTERNAL", message: "internal server error" } };
    return c.json(body, 500);
  });

  return app;
}
```

- [ ] **Step 6: `server.ts` を全置換（authConfig を配線）**

```ts
import { serve } from "@hono/node-server";
import { createApp } from "./app";
import { resolveAuthConfig } from "./lib/auth-config";
import { resolveNasRoot } from "./lib/config";

const root = resolveNasRoot();
const authConfig = resolveAuthConfig();
const app = createApp(root, authConfig);
const port = 8080;

serve({ fetch: app.fetch, hostname: "0.0.0.0", port }, (info) => {
  console.log(`Server listening on http://0.0.0.0:${info.port} (NAS_ROOT: ${root})`);
});
```

- [ ] **Step 7: 既存の files.routes.test.ts に認証 Cookie を付与**

`apps/server/src/features/files/files.routes.test.ts` は `createApp(root)` を呼び認証なしで `/api/*` を叩いているため、認証導入で全て 401 になる。以下を機械的に適用する:

(a) import に追加（先頭の import 群）:
```ts
import type { AuthConfig } from "../../lib/auth-config";
import { hashPassword } from "../../lib/password";
import { issueToken } from "../auth/auth.service";
```
(b) `let root: string;` の近くに認証ヘルパを追加:
```ts
const authConfig: AuthConfig = { secret: "test-secret", passwordHash: hashPassword("pw") };
let authCookie: string;

beforeEach(async () => {
  authCookie = `nasfm_token=${await issueToken(authConfig)}`;
});

function withAuth(init: RequestInit = {}): RequestInit {
  return { ...init, headers: { ...(init.headers ?? {}), Cookie: authCookie } };
}
```
（既存の `beforeEach` が root を作っているなら、その中に `authCookie = ...` を足す形でよい。`beforeEach` は複数あっても順に実行される。）
(c) すべての `createApp(root)` を `createApp(root, authConfig)` に置換する。
(d) すべての `app.request(<path>)` / `app.request(<path>, <init>)` を認証ヘッダ付きに置換する。第2引数が無い GET は `app.request(<path>, withAuth())`、既に init があるものは `app.request(<path>, withAuth(<init>))` にする。**ただし `/health` を叩くテストがあれば認証は不要**（あってもガード対象外なので害はないが、原則どおり素のままでよい）。

置換後、全リクエストが有効な Cookie を持つため既存の期待ステータス（200/201/400/404/409）は元のまま通る。

- [ ] **Step 8: auth の .gitkeep を削除して検証・コミット**

```bash
rm -f apps/server/src/features/auth/.gitkeep
npm run test -w @nas-fm/server
npm run typecheck
git add -A
git commit -m "feat: 認証ミドルウェアと /api/auth ルートで API を保護"
```
Expected: server 全テスト PASS（auth.routes.test 含む、files.routes.test も Cookie 付きで PASS）、typecheck 0。

---

### Task 6: web API クライアントとエラーメッセージ

**Files:**
- Modify: `apps/web/src/lib/api.ts` + `api.test.ts`, `apps/web/src/lib/error-messages.ts` + `error-messages.test.ts`

**Interfaces:**
- Consumes: `@nas-fm/shared` の `AuthStatus`（型）
- Produces: `api.login(password: string): Promise<void>`、`api.logout(): Promise<void>`、`api.me(): Promise<AuthStatus>`。`errorMessage("UNAUTHORIZED")` = 「ログインが必要です」

- [ ] **Step 1: error-messages のテストに UNAUTHORIZED を追加**

`apps/web/src/lib/error-messages.test.ts` の `it.each` の配列に `"UNAUTHORIZED"` を追加（他コードと並べる）:
```ts
    "UNAUTHORIZED",
```

- [ ] **Step 2: api のテストに login/logout/me を追加**

`apps/web/src/lib/api.test.ts` の末尾（最後の `describe` の後）に追加:
```ts
describe("api.login / logout / me", () => {
  it("login は password を JSON で POST する", async () => {
    mockFetch(200, { ok: true });
    await api.login("secret");
    expect(fetch).toHaveBeenCalledWith(
      "/api/auth/login",
      expect.objectContaining({ method: "POST" }),
    );
  });

  it("login 失敗は ApiRequestError", async () => {
    mockFetch(401, { error: { code: "UNAUTHORIZED", message: "invalid" } });
    await expect(api.login("bad")).rejects.toMatchObject({ code: "UNAUTHORIZED" });
  });

  it("logout は POST する", async () => {
    mockFetch(200, { ok: true });
    await api.logout();
    expect(fetch).toHaveBeenCalledWith(
      "/api/auth/logout",
      expect.objectContaining({ method: "POST" }),
    );
  });

  it("me は AuthStatus を返す", async () => {
    mockFetch(200, { authenticated: true });
    expect(await api.me()).toEqual({ authenticated: true });
  });
});
```

- [ ] **Step 3: テストが失敗することを確認**

Run: `npm run test -w @nas-fm/web`
Expected: FAIL（`api.login` 等が無い、UNAUTHORIZED メッセージが無い）

- [ ] **Step 4: `error-messages.ts` に UNAUTHORIZED を追加**

`apps/web/src/lib/error-messages.ts` の `MESSAGES` に追加（`INTERNAL` の前）:
```ts
  UNAUTHORIZED: "ログインが必要です",
```

- [ ] **Step 5: `api.ts` に login/logout/me を追加**

`apps/web/src/lib/api.ts` の import に `AuthStatus` を追加:
```ts
import type { AuthStatus, ListResponse } from "@nas-fm/shared";
```
`api` オブジェクトに以下のメソッドを追加（`downloadUrl` の後など）:
```ts
  async login(password: string): Promise<void> {
    await request("/api/auth/login", {
      method: "POST",
      headers: JSON_HEADERS,
      body: JSON.stringify({ password }),
    });
  },

  async logout(): Promise<void> {
    await request("/api/auth/logout", { method: "POST" });
  },

  async me(): Promise<AuthStatus> {
    const res = await request("/api/auth/me");
    return (await res.json()) as AuthStatus;
  },
```

- [ ] **Step 6: テスト・typecheck を確認してコミット**

```bash
npm run test -w @nas-fm/web
npm run typecheck -w @nas-fm/web
git add apps/web/src/lib/api.ts apps/web/src/lib/api.test.ts apps/web/src/lib/error-messages.ts apps/web/src/lib/error-messages.test.ts
git commit -m "feat: Web の認証APIクライアントとエラーメッセージを追加"
```
Expected: 全 PASS、typecheck 0。

---

### Task 7: web 認証 feature（useAuth / LoginForm / LogoutButton / AuthGate）

**Files:**
- Create: `apps/web/src/features/auth/hooks/useAuth.ts`, `components/LoginForm.tsx`, `components/LogoutButton.tsx`, `components/AuthGate.tsx`, `index.ts`, および対応テスト
- Delete: `apps/web/src/features/auth/.gitkeep`

**Interfaces:**
- Consumes: `api`（login/logout/me）, `toast`（sonner）, shadcn `button`/`input`/`label`/`card`
- Produces:
  - `useAuth()` → `useQuery(["me"])` の結果（`data: AuthStatus | undefined`, `isLoading`）
  - `LoginForm()`（パスワード入力 → `api.login` → `["me"]` 無効化、失敗トースト）
  - `LogoutButton()`（`api.logout` → `["me"]` 無効化）
  - `AuthGate({ children })`（`useAuth`: 未認証は `LoginForm`、認証済みは children）
  - `index.ts` が `AuthGate` / `LogoutButton` / `useAuth` を公開

- [ ] **Step 1: useAuth のテストを書く**

`apps/web/src/features/auth/hooks/useAuth.test.tsx`:
```tsx
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { renderHook, waitFor } from "@testing-library/react";
import type { ReactNode } from "react";
import { afterEach, describe, expect, it, vi } from "vitest";
import { api } from "@/lib/api";
import { useAuth } from "./useAuth";

function wrapper({ children }: { children: ReactNode }) {
  const client = new QueryClient({ defaultOptions: { queries: { retry: false } } });
  return <QueryClientProvider client={client}>{children}</QueryClientProvider>;
}

afterEach(() => vi.restoreAllMocks());

describe("useAuth", () => {
  it("認証状態を取得する", async () => {
    vi.spyOn(api, "me").mockResolvedValue({ authenticated: true });
    const { result } = renderHook(() => useAuth(), { wrapper });
    await waitFor(() => expect(result.current.data).toEqual({ authenticated: true }));
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `npm run test -w @nas-fm/web`
Expected: FAIL（`./useAuth` が無い）

- [ ] **Step 3: `hooks/useAuth.ts` を実装**

```ts
import { useQuery } from "@tanstack/react-query";
import { api } from "@/lib/api";

export function useAuth() {
  return useQuery({ queryKey: ["me"], queryFn: () => api.me() });
}
```

- [ ] **Step 4: LoginForm / AuthGate のテストを書く**

`apps/web/src/features/auth/components/LoginForm.test.tsx`:
```tsx
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import type { ReactNode } from "react";
import { afterEach, describe, expect, it, vi } from "vitest";
import { api } from "@/lib/api";
import { LoginForm } from "./LoginForm";

function renderWithClient(ui: ReactNode) {
  const client = new QueryClient({ defaultOptions: { mutations: { retry: false } } });
  return render(<QueryClientProvider client={client}>{ui}</QueryClientProvider>);
}

afterEach(() => vi.restoreAllMocks());

describe("LoginForm", () => {
  it("パスワードを入力してログインすると api.login を呼ぶ", async () => {
    const login = vi.spyOn(api, "login").mockResolvedValue();
    renderWithClient(<LoginForm />);
    await userEvent.type(screen.getByLabelText("パスワード"), "secret");
    await userEvent.click(screen.getByRole("button", { name: "ログイン" }));
    expect(login).toHaveBeenCalledWith("secret");
  });
});
```

`apps/web/src/features/auth/components/AuthGate.test.tsx`:
```tsx
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { render, screen, waitFor } from "@testing-library/react";
import type { ReactNode } from "react";
import { afterEach, describe, expect, it, vi } from "vitest";
import { api } from "@/lib/api";
import { AuthGate } from "./AuthGate";

function renderWithClient(ui: ReactNode) {
  const client = new QueryClient({ defaultOptions: { queries: { retry: false } } });
  return render(<QueryClientProvider client={client}>{ui}</QueryClientProvider>);
}

afterEach(() => vi.restoreAllMocks());

describe("AuthGate", () => {
  it("認証済みなら children を表示する", async () => {
    vi.spyOn(api, "me").mockResolvedValue({ authenticated: true });
    renderWithClient(
      <AuthGate>
        <p>protected</p>
      </AuthGate>,
    );
    await waitFor(() => expect(screen.getByText("protected")).toBeInTheDocument());
  });

  it("未認証ならログインフォームを表示する", async () => {
    vi.spyOn(api, "me").mockResolvedValue({ authenticated: false });
    renderWithClient(
      <AuthGate>
        <p>protected</p>
      </AuthGate>,
    );
    await waitFor(() => expect(screen.getByRole("button", { name: "ログイン" })).toBeInTheDocument());
    expect(screen.queryByText("protected")).toBeNull();
  });
});
```

- [ ] **Step 5: LoginForm / LogoutButton / AuthGate を実装**

`apps/web/src/features/auth/components/LoginForm.tsx`:
```tsx
import { useMutation, useQueryClient } from "@tanstack/react-query";
import { useState } from "react";
import { toast } from "sonner";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { api } from "@/lib/api";

export function LoginForm() {
  const qc = useQueryClient();
  const [password, setPassword] = useState("");
  const login = useMutation({
    mutationFn: (pw: string) => api.login(pw),
    onSuccess: () => qc.invalidateQueries({ queryKey: ["me"] }),
    onError: () => toast.error("パスワードが違います"),
  });

  return (
    <div className="flex justify-center pt-16">
      <Card className="w-full max-w-sm space-y-4 p-6">
        <h2 className="text-lg font-semibold">ログイン</h2>
        <form
          className="space-y-4"
          onSubmit={(e) => {
            e.preventDefault();
            login.mutate(password);
          }}
        >
          <div className="space-y-2">
            <Label htmlFor="login-password">パスワード</Label>
            <Input
              id="login-password"
              type="password"
              value={password}
              onChange={(e) => setPassword(e.target.value)}
              autoFocus
            />
          </div>
          <Button type="submit" className="w-full" disabled={login.isPending}>
            ログイン
          </Button>
        </form>
      </Card>
    </div>
  );
}
```

`apps/web/src/features/auth/components/LogoutButton.tsx`:
```tsx
import { useMutation, useQueryClient } from "@tanstack/react-query";
import { LogOut } from "lucide-react";
import { Button } from "@/components/ui/button";
import { api } from "@/lib/api";

export function LogoutButton() {
  const qc = useQueryClient();
  const logout = useMutation({
    mutationFn: () => api.logout(),
    onSuccess: () => qc.invalidateQueries({ queryKey: ["me"] }),
  });
  return (
    <Button variant="outline" size="sm" onClick={() => logout.mutate()} disabled={logout.isPending}>
      <LogOut size={16} className="mr-2" />
      ログアウト
    </Button>
  );
}
```

`apps/web/src/features/auth/components/AuthGate.tsx`:
```tsx
import type { ReactNode } from "react";
import { useAuth } from "../hooks/useAuth";
import { LoginForm } from "./LoginForm";

export function AuthGate({ children }: { children: ReactNode }) {
  const { data, isLoading } = useAuth();
  if (isLoading) {
    return <p className="text-muted-foreground">読み込み中…</p>;
  }
  if (!data?.authenticated) {
    return <LoginForm />;
  }
  return <>{children}</>;
}
```

- [ ] **Step 6: `index.ts` を作成**

`apps/web/src/features/auth/index.ts`:
```ts
export { AuthGate } from "./components/AuthGate";
export { LogoutButton } from "./components/LogoutButton";
export { useAuth } from "./hooks/useAuth";
```

- [ ] **Step 7: .gitkeep 削除・検証・コミット**

```bash
rm -f apps/web/src/features/auth/.gitkeep
npm run test -w @nas-fm/web
npm run typecheck -w @nas-fm/web
git add -A
git commit -m "feat: ログイン画面と認証ゲートを追加"
```
Expected: 全 PASS、typecheck 0。

---

### Task 8: App 統合と 401 セッション切れ処理

**Files:**
- Modify: `apps/web/src/app/providers.tsx` + 新規 `providers.test.ts`, `apps/web/src/app/App.tsx`, `apps/web/src/app/App.test.tsx`

**Interfaces:**
- Consumes: `AuthGate` / `LogoutButton`（`@/features/auth`）, `FileBrowser`（`@/features/file-list`）, `ApiRequestError`（`@/lib/api`）
- Produces: `Providers` が `QueryCache`/`MutationCache` の onError で `UNAUTHORIZED` を検知し `["me"]` を無効化。`createAuthAwareQueryClient(): QueryClient`（テスト用に export）

- [ ] **Step 1: providers の 401 ハンドラのテストを書く**

`apps/web/src/app/providers.test.ts`:
```ts
import { describe, expect, it, vi } from "vitest";
import { ApiRequestError } from "@/lib/api";
import { createAuthAwareQueryClient } from "./providers";

describe("createAuthAwareQueryClient", () => {
  it("UNAUTHORIZED エラーで ['me'] を無効化する", async () => {
    const client = createAuthAwareQueryClient();
    const spy = vi.spyOn(client, "invalidateQueries");
    client.getMutationCache().config.onError?.(
      new ApiRequestError("UNAUTHORIZED", "x"),
      undefined,
      undefined,
      // biome/oxlint 対策で any を避けるためのダミー mutation
      { options: {} } as never,
    );
    expect(spy).toHaveBeenCalledWith({ queryKey: ["me"] });
  });

  it("他のエラーでは無効化しない", async () => {
    const client = createAuthAwareQueryClient();
    const spy = vi.spyOn(client, "invalidateQueries");
    client.getMutationCache().config.onError?.(
      new ApiRequestError("CONFLICT", "x"),
      undefined,
      undefined,
      { options: {} } as never,
    );
    expect(spy).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `npm run test -w @nas-fm/web`
Expected: FAIL（`createAuthAwareQueryClient` が無い）

- [ ] **Step 3: `providers.tsx` を全置換（中央 401 ハンドラ）**

```tsx
import { MutationCache, QueryCache, QueryClient, QueryClientProvider } from "@tanstack/react-query";
import type { ReactNode } from "react";
import { Toaster } from "@/components/ui/sonner";
import { ApiRequestError } from "@/lib/api";

export function createAuthAwareQueryClient(): QueryClient {
  const onAuthError = (error: unknown) => {
    if (error instanceof ApiRequestError && error.code === "UNAUTHORIZED") {
      client.invalidateQueries({ queryKey: ["me"] });
    }
  };
  const client: QueryClient = new QueryClient({
    queryCache: new QueryCache({ onError: onAuthError }),
    mutationCache: new MutationCache({ onError: onAuthError }),
  });
  return client;
}

const queryClient = createAuthAwareQueryClient();

export function Providers({ children }: { children: ReactNode }) {
  return (
    <QueryClientProvider client={queryClient}>
      {children}
      <Toaster richColors position="top-right" />
    </QueryClientProvider>
  );
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `npm run test -w @nas-fm/web`
Expected: PASS（providers.test の2ケース）。

- [ ] **Step 5: `App.tsx` を全置換（AuthGate + LogoutButton）**

```tsx
import { AuthGate, LogoutButton } from "@/features/auth";
import { FileBrowser } from "@/features/file-list";
import { Providers } from "./providers";

export function App() {
  return (
    <Providers>
      <div className="min-h-screen bg-background text-foreground">
        <header className="flex items-center justify-between border-b px-6 py-4">
          <h1 className="text-xl font-semibold">NAS-FileManager</h1>
        </header>
        <main className="p-6">
          <AuthGate>
            <div className="mb-4 flex justify-end">
              <LogoutButton />
            </div>
            <FileBrowser />
          </AuthGate>
        </main>
      </div>
    </Providers>
  );
}
```

- [ ] **Step 6: `App.test.tsx` を全置換（me をモック）**

```tsx
import { render, screen, waitFor } from "@testing-library/react";
import { afterEach, describe, expect, it, vi } from "vitest";
import { api } from "@/lib/api";
import { App } from "./App";

afterEach(() => vi.restoreAllMocks());

describe("App", () => {
  it("ヘッダにアプリ名を表示する", () => {
    vi.spyOn(api, "me").mockResolvedValue({ authenticated: false });
    render(<App />);
    expect(screen.getByRole("heading", { name: "NAS-FileManager" })).toBeInTheDocument();
  });

  it("未認証ではログインフォームを表示する", async () => {
    vi.spyOn(api, "me").mockResolvedValue({ authenticated: false });
    render(<App />);
    await waitFor(() => expect(screen.getByRole("button", { name: "ログイン" })).toBeInTheDocument());
  });

  it("認証済みでは一覧（ログアウト）を表示する", async () => {
    vi.spyOn(api, "me").mockResolvedValue({ authenticated: true });
    vi.spyOn(api, "list").mockResolvedValue({ path: "", entries: [] });
    render(<App />);
    await waitFor(() => expect(screen.getByRole("button", { name: "ログアウト" })).toBeInTheDocument());
  });
});
```

- [ ] **Step 7: 検証してコミット**

```bash
npm run test -w @nas-fm/web
npm run typecheck -w @nas-fm/web
git add -A
git commit -m "feat: 認証ゲートで UI を保護し 401 でログインへ戻す"
```
Expected: 全 PASS、typecheck 0。

---

### Task 9: 全体検証・実疎通・ロードマップ更新

**Files:**
- Modify: `docs/roadmap.md`

- [ ] **Step 1: ルートで全チェック**

```bash
npm run typecheck && npm run test && npm run lint && npm run fmt:check && npm run build
```
Expected: すべて成功（server + web 全テスト、build で dist 生成）。`fmt:check` が差分を出したら `npm run fmt` して再確認。

- [ ] **Step 2: dev で実疎通（Node fetch。curl 禁止）**

`npm run dev:server` をバックグラウンド起動し、listen ログが出たら以下で認証フローを確認する:
```bash
node -e "
const base='http://127.0.0.1:8080';
const json={'content-type':'application/json'};
(async()=>{
  let r=await fetch(base+'/api/list?path=');
  console.log('no-auth list', r.status); // 401 期待
  r=await fetch(base+'/api/auth/login',{method:'POST',headers:json,body:JSON.stringify({password:'admin'})});
  console.log('login', r.status);        // 200 期待（開発デフォルト admin）
  const cookie=(r.headers.get('set-cookie')||'').split(';')[0];
  r=await fetch(base+'/api/auth/me',{headers:{Cookie:cookie}});
  console.log('me', r.status, await r.text()); // {authenticated:true}
  r=await fetch(base+'/api/list?path=',{headers:{Cookie:cookie}});
  console.log('auth list', r.status);    // 200 期待
  r=await fetch(base+'/api/auth/login',{method:'POST',headers:json,body:JSON.stringify({password:'wrong'})});
  console.log('bad login', r.status);    // 401 期待
})();
"
```
Expected:
```
no-auth list 401
login 200
me 200 {"authenticated":true}
auth list 200
bad login 401
```
確認後、バックグラウンドのサーバを必ず停止する。（起動ログに開発用認証の WARNING が出ることも確認）

- [ ] **Step 3: `docs/roadmap.md` の Phase 2 を更新**

Phase 2 セクションの2つの `- [ ]` を `- [x]` にする。さらに Phase 3 の `auth` feature 行（「（Phase 2 実装後）」注記付き）があれば `- [x]` にして注記を外す。

- [ ] **Step 4: コミット**

```bash
git add docs/roadmap.md
git commit -m "chore: Phase 2 認証の完了に合わせてロードマップを更新"
```

---

## Self-Review（実施済み）

**1. Spec coverage:** 設計 spec の各項目→タスク対応 — httpOnly Cookie+JWT（T4,T5）/ scrypt ハッシュ＋CLI（T2）/ auth-config 開発デフォルト＋警告（T3）/ login・logout・me（T5）/ ルート保護（T5、createApp のガード）/ UNAUTHORIZED→401（T1）/ 共有型（T1）/ web api（T6）/ ログイン画面・AuthGate・ログアウト（T7）/ 401 セッション切れ（T8 中央ハンドラ）/ エラーメッセージ（T6）/ テスト方針（全タスク）。ギャップなし。

**2. Placeholder scan:** TBD/TODO なし。全コードステップに実コード。T5 Step7 の既存テスト更新のみ「機械的置換」の手順記述だが、追加する正確なコードと置換規則を明示しており曖昧さはない（300行の再掲を避けるため）。

**3. Type consistency:** `AuthConfig{secret,passwordHash}` / `resolveAuthConfig()` / `hashPassword`/`verifyPassword` / `verifyLogin(config,pw)`/`issueToken(config)`/`verifyToken(config,token)` / `requireAuth(config)` / `COOKIE_NAME` / `createAuthRoutes(config)` / `createApp(root, authConfig)` / `TOKEN_TTL_SECONDS` / web `api.login/logout/me`・`AuthStatus`・`createAuthAwareQueryClient` を全タスク間で照合。`statusOf` 戻り型に 401 追加を T1 で反映。整合。

**設計からの精緻化（重要）:** 設計 §3.1 は「files サブアプリが自己保護」としていたが、files を `/api` にマウントすると catch-all ミドルウェアが `/api/auth/*` も捕捉してしまう。本計画では **createApp 側で `/api/auth/*` を除外するガード**（`app.use("/api/*", …)` を auth ルート登録の後に置く）に変更し、`createFilesRoutes` は無変更とした（Phase 1 の files 実装・大半のテストに影響しない）。401 セッション切れは各フックではなく **QueryClient の中央 onError** に集約（T8）。挙動は設計の意図どおり。
