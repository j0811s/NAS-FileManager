# 削除の取り消し（ゴミ箱） Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `DELETE /api/delete` を物理削除からソフトデリート（`.trash/` への移動）に変え、復元・完全削除ができるゴミ箱画面を追加する。ゴミ箱内は30日で自動的に完全削除される。

**Architecture:** サーバに新規 feature `trash` を追加し、`NAS_ROOT/.trash/<uuid>/<basename>` + `<uuid>.json`（メタデータ）でソフトデリートを実装する。既存の `files` feature の `DELETE /delete` はこの `trash` feature の `moveToTrash` に処理を委譲し、`listDir` とフォルダzipダウンロードから `.trash` を除外する。フロントは新規 feature `trash` にゴミ箱一覧・復元・完全削除の React Query フックとダイアログを実装し、ヘッダーのアイコンボタンから開く。

**Tech Stack:** Hono（サーバ）、`node:crypto`（`randomUUID`）、React + `@tanstack/react-query`、shadcn/ui（`Dialog` / `AlertDialog` / `Table`）、Vitest + Testing Library。新規依存パッケージの追加なし。

## Global Constraints

- 対象 spec: `docs/superpowers/specs/2026-07-13-trash-design.md`
- `DELETE /api/delete` のエンドポイント自体（URL・レスポンス形式）は変更しない。内部実装のみソフトデリートに差し替える
- ゴミ箱の実データは `NAS_ROOT/.trash/` に置く（`.trash/<uuid>/<basename>` + `.trash/<uuid>.json`）
- 保持期間は30日。バックグラウンドジョブは持たず、`GET /api/trash` が呼ばれるたびに期限切れ分を掃除する遅延パージ
- 「ゴミ箱を空にする」一括操作・`.trash/*` への直接アクセス防止・復元時の自動リネームはスコープ外（spec の「スコープ外」節）
- feature 間の import は各 feature の `index.ts`（公開境界）経由のみ（フロント側。サーバ側は既存コードに合わせ他 feature のモジュールを直接 import する）
- 型のみの import/export は `import type` / `export type`（`verbatimModuleSyntax: true`）
- 新規依存は追加しない
- コミットメッセージは Conventional Commits（接頭辞は英語、本文は日本語）

**前提（重要）:** このプランは `docs/superpowers/plans/2026-07-13-disk-usage-display.md`（ディスク使用量表示）が**既に実装済み**であることを前提とする（Task 9 の `["disk-usage"]` invalidate、Task 12 の `App.tsx` 全体置き換えが `@/features/disk-usage` の `DiskUsageBadge` に依存する）。未実装の状態でこのプランだけを進める場合は、Task 12 で `App.tsx` に `DiskUsageBadge` 関連の行を含めない（`TrashButton` のみ追加する）よう読み替えること。

---

### Task 1: shared に `TrashEntry` / `TrashListResponse` / `TrashRestoreRequest` を追加

**Files:**
- Modify: `packages/shared/src/types.ts`
- Modify: `packages/shared/src/index.ts`

**Interfaces:**
- Produces: `TrashEntry { id: string; name: string; originalPath: string; type: FileType; size: number; deletedAt: number }`、`TrashListResponse { entries: TrashEntry[] }`、`TrashRestoreRequest { id: string }`（いずれも `@nas-fm/shared` からの named export）

- [ ] **Step 1: `packages/shared/src/types.ts` の末尾に型を追加**

```ts
export interface TrashEntry {
  id: string;
  name: string;
  originalPath: string;
  type: FileType;
  size: number;
  /** 削除時刻（epoch ミリ秒） */
  deletedAt: number;
}

export interface TrashListResponse {
  entries: TrashEntry[];
}

export interface TrashRestoreRequest {
  id: string;
}
```

- [ ] **Step 2: `packages/shared/src/index.ts` の export type 一覧に追加**

```ts
export type {
  ApiError,
  ApiErrorCode,
  AuthStatus,
  DiskUsageResponse,
  FileEntry,
  FileType,
  ListResponse,
  LoginRequest,
  MkdirRequest,
  OkResponse,
  RenameRequest,
  TrashEntry,
  TrashListResponse,
  TrashRestoreRequest,
} from "./types";
```

（`DiskUsageResponse` は既にディスク使用量表示機能で追加済みの前提。未追加ならこの並びで一緒に加える。）

- [ ] **Step 3: 型チェックが通ることを確認**

Run: `npm run typecheck -w @nas-fm/shared`
Expected: エラーなし

- [ ] **Step 4: Commit**

```bash
git add packages/shared/src/types.ts packages/shared/src/index.ts
git commit -m "$(cat <<'EOF'
feat: TrashEntry等のゴミ箱関連型を追加

EOF
)"
```

---

### Task 2: `trash.service.ts` — `moveToTrash`

**Files:**
- Create: `apps/server/src/features/trash/trash.service.ts`
- Test: `apps/server/src/features/trash/trash.service.test.ts`

**Interfaces:**
- Produces: `TRASH_DIR_NAME = ".trash"`（定数、Task 6 が消費）、`moveToTrash(root: string, relPath: string): Promise<void>`（Task 5 が消費）

- [ ] **Step 1: 失敗するテストを書く**

`apps/server/src/features/trash/trash.service.test.ts` を新規作成:

```ts
import { mkdir, mkdtemp, readdir, readFile, rm, writeFile } from "node:fs/promises";
import { tmpdir } from "node:os";
import path from "node:path";
import { afterEach, beforeEach, describe, expect, it } from "vitest";
import { AppError } from "../../lib/errors";
import { moveToTrash, TRASH_DIR_NAME } from "./trash.service";

let root: string;

beforeEach(async () => {
  root = await mkdtemp(path.join(tmpdir(), "nasfm-trash-"));
});

afterEach(async () => {
  await rm(root, { recursive: true, force: true });
});

async function expectAppError(promise: Promise<unknown>, code: string): Promise<void> {
  let caught: unknown;
  try {
    await promise;
  } catch (err) {
    caught = err;
  }
  expect(caught).toBeInstanceOf(AppError);
  expect((caught as AppError).code).toBe(code);
}

describe("moveToTrash", () => {
  it("ファイルを .trash/<uuid>/ に移動し、元の場所から消す", async () => {
    await writeFile(path.join(root, "a.txt"), "hello");
    await moveToTrash(root, "a.txt");

    const remaining = (await readdir(root)).filter((n) => n !== TRASH_DIR_NAME);
    expect(remaining).toEqual([]);

    const ids = await readdir(path.join(root, TRASH_DIR_NAME));
    const dirId = ids.find((n) => !n.endsWith(".json"));
    expect(dirId).toBeDefined();
    const movedName = await readdir(path.join(root, TRASH_DIR_NAME, dirId as string));
    expect(movedName).toEqual(["a.txt"]);

    const meta = JSON.parse(
      await readFile(path.join(root, TRASH_DIR_NAME, `${dirId}.json`), "utf8"),
    ) as { originalPath: string; deletedAt: number };
    expect(meta.originalPath).toBe("a.txt");
    expect(meta.deletedAt).toBeGreaterThan(0);
  });

  it("空でないディレクトリを再帰的に移動できる", async () => {
    await mkdir(path.join(root, "sub"));
    await writeFile(path.join(root, "sub/b.txt"), "x");
    await moveToTrash(root, "sub");

    const remaining = (await readdir(root)).filter((n) => n !== TRASH_DIR_NAME);
    expect(remaining).toEqual([]);

    const ids = await readdir(path.join(root, TRASH_DIR_NAME));
    const dirId = ids.find((n) => !n.endsWith(".json")) as string;
    const inner = await readdir(path.join(root, TRASH_DIR_NAME, dirId, "sub"));
    expect(inner).toEqual(["b.txt"]);
  });

  it("存在しないパスは NOT_FOUND", async () => {
    await expectAppError(moveToTrash(root, "missing"), "NOT_FOUND");
  });

  it("root 自身の削除は INVALID_REQUEST", async () => {
    await expectAppError(moveToTrash(root, ""), "INVALID_REQUEST");
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `npm run test -w @nas-fm/server -- trash.service`
Expected: FAIL（`trash.service` モジュールが存在しない）

- [ ] **Step 3: 最小実装を書く**

`apps/server/src/features/trash/trash.service.ts` を新規作成:

```ts
import { randomUUID } from "node:crypto";
import fs from "node:fs/promises";
import path from "node:path";
import { AppError, fromFsError } from "../../lib/errors";
import { safeResolve } from "../../lib/safe-resolve";

export const TRASH_DIR_NAME = ".trash";

function trashRoot(root: string): string {
  return path.join(root, TRASH_DIR_NAME);
}

interface TrashMeta {
  originalPath: string;
  deletedAt: number;
}

export async function moveToTrash(root: string, relPath: string): Promise<void> {
  const abs = safeResolve(root, relPath);
  if (abs === root) {
    throw new AppError("INVALID_REQUEST", "cannot delete the root directory");
  }
  const st = await fs.lstat(abs).catch(() => null);
  if (!st) {
    throw new AppError("NOT_FOUND", `not found: ${relPath}`);
  }

  const id = randomUUID();
  const itemDir = path.join(trashRoot(root), id);
  await fs.mkdir(itemDir, { recursive: true });
  const basename = path.basename(abs);
  try {
    await fs.rename(abs, path.join(itemDir, basename));
  } catch (err) {
    await fs.rm(itemDir, { recursive: true, force: true }).catch(() => undefined);
    throw fromFsError(err, relPath);
  }
  const meta: TrashMeta = { originalPath: relPath, deletedAt: Date.now() };
  await fs.writeFile(path.join(trashRoot(root), `${id}.json`), JSON.stringify(meta));
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `npm run test -w @nas-fm/server -- trash.service`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add apps/server/src/features/trash/trash.service.ts apps/server/src/features/trash/trash.service.test.ts
git commit -m "$(cat <<'EOF'
feat: ファイルを.trashへ移動するmoveToTrashを追加

EOF
)"
```

---

### Task 3: `trash.service.ts` — `listTrash`（自動パージ込み）

**Files:**
- Modify: `apps/server/src/features/trash/trash.service.ts`
- Modify: `apps/server/src/features/trash/trash.service.test.ts`

**Interfaces:**
- Consumes: `moveToTrash`（Task 2、テストのセットアップに使用）、`TRASH_DIR_NAME`（Task 2）
- Produces: `listTrash(root: string): Promise<TrashEntry[]>`（Task 6 が消費）

- [ ] **Step 1: 失敗するテストを書く**

`apps/server/src/features/trash/trash.service.test.ts` の先頭の import を変更し、`listTrash` を追加:

```ts
import { listTrash, moveToTrash, TRASH_DIR_NAME } from "./trash.service";
```

続けて、末尾（既存 `describe("moveToTrash", ...)` の後）に追加:

```ts
describe("listTrash", () => {
  it("移動した項目が一覧に出る", async () => {
    await writeFile(path.join(root, "a.txt"), "hello");
    await moveToTrash(root, "a.txt");

    const entries = await listTrash(root);
    expect(entries).toHaveLength(1);
    expect(entries[0]).toMatchObject({
      name: "a.txt",
      originalPath: "a.txt",
      type: "file",
      size: 5,
    });
    expect(entries[0].deletedAt).toBeGreaterThan(0);
    expect(typeof entries[0].id).toBe("string");
  });

  it("フォルダを移動した場合は type が dir、size は 0", async () => {
    await mkdir(path.join(root, "sub"));
    await writeFile(path.join(root, "sub/b.txt"), "x");
    await moveToTrash(root, "sub");

    const entries = await listTrash(root);
    expect(entries[0]).toMatchObject({ name: "sub", type: "dir", size: 0 });
  });

  it("30日を超えたエントリは自動的に完全削除され、一覧に出ない", async () => {
    await writeFile(path.join(root, "a.txt"), "hello");
    await moveToTrash(root, "a.txt");
    const [before] = await listTrash(root);

    const metaPath = path.join(root, TRASH_DIR_NAME, `${before.id}.json`);
    const old = Date.now() - 31 * 24 * 60 * 60 * 1000;
    await writeFile(metaPath, JSON.stringify({ originalPath: "a.txt", deletedAt: old }));

    const entries = await listTrash(root);
    expect(entries).toEqual([]);
    const remainingIds = await readdir(path.join(root, TRASH_DIR_NAME));
    expect(remainingIds).toEqual([]);
  });

  it("壊れたメタデータ(不正JSON)は無視してクラッシュしない", async () => {
    await mkdir(path.join(root, TRASH_DIR_NAME), { recursive: true });
    await writeFile(path.join(root, TRASH_DIR_NAME, "broken.json"), "{not json");

    const entries = await listTrash(root);
    expect(entries).toEqual([]);
  });

  it(".trash ディレクトリが無ければ空配列を返す", async () => {
    const entries = await listTrash(root);
    expect(entries).toEqual([]);
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `npm run test -w @nas-fm/server -- trash.service`
Expected: FAIL（`listTrash` が未定義）

- [ ] **Step 3: 実装を追加**

`apps/server/src/features/trash/trash.service.ts` に import を追記し（先頭の import ブロックを変更）、関数を追加:

```ts
import { randomUUID } from "node:crypto";
import fs from "node:fs/promises";
import path from "node:path";
import type { TrashEntry } from "@nas-fm/shared";
import { AppError, fromFsError } from "../../lib/errors";
import { safeResolve } from "../../lib/safe-resolve";
```

`TRASH_DIR_NAME` の定義の下、`TrashMeta` interface の下に追記:

```ts
const RETENTION_MS = 30 * 24 * 60 * 60 * 1000;

async function readMeta(metaPath: string): Promise<TrashMeta | null> {
  try {
    const raw = await fs.readFile(metaPath, "utf8");
    const parsed = JSON.parse(raw) as Partial<TrashMeta>;
    if (typeof parsed.originalPath !== "string" || typeof parsed.deletedAt !== "number") {
      return null;
    }
    return { originalPath: parsed.originalPath, deletedAt: parsed.deletedAt };
  } catch {
    return null;
  }
}

async function removeTrashFiles(root: string, id: string): Promise<void> {
  const dir = trashRoot(root);
  await fs.rm(path.join(dir, id), { recursive: true, force: true });
  await fs.rm(path.join(dir, `${id}.json`), { force: true });
}

export async function listTrash(root: string): Promise<TrashEntry[]> {
  const dir = trashRoot(root);
  const names = await fs.readdir(dir).catch((err) => {
    if ((err as NodeJS.ErrnoException).code === "ENOENT") return [];
    throw fromFsError(err, TRASH_DIR_NAME);
  });
  const ids = names.filter((n) => n.endsWith(".json")).map((n) => n.slice(0, -".json".length));

  const entries: TrashEntry[] = [];
  for (const id of ids) {
    const meta = await readMeta(path.join(dir, `${id}.json`));
    if (!meta) continue;

    if (Date.now() - meta.deletedAt > RETENTION_MS) {
      await removeTrashFiles(root, id);
      continue;
    }

    const itemDir = path.join(dir, id);
    const itemNames = await fs.readdir(itemDir).catch(() => []);
    const name = itemNames[0];
    if (!name) continue;

    const st = await fs.stat(path.join(itemDir, name)).catch(() => null);
    if (!st) continue;

    entries.push({
      id,
      name,
      originalPath: meta.originalPath,
      type: st.isDirectory() ? "dir" : "file",
      size: st.isDirectory() ? 0 : st.size,
      deletedAt: meta.deletedAt,
    });
  }
  entries.sort((a, b) => b.deletedAt - a.deletedAt);
  return entries;
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `npm run test -w @nas-fm/server -- trash.service`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add apps/server/src/features/trash/trash.service.ts apps/server/src/features/trash/trash.service.test.ts
git commit -m "$(cat <<'EOF'
feat: 30日超のエントリを自動パージするlistTrashを追加

EOF
)"
```

---

### Task 4: `trash.service.ts` — `restoreFromTrash` / `purgeTrashEntry`

**Files:**
- Modify: `apps/server/src/features/trash/trash.service.ts`
- Modify: `apps/server/src/features/trash/trash.service.test.ts`

**Interfaces:**
- Consumes: `moveToTrash` / `listTrash`（Task 2, 3。テストのセットアップに使用）
- Produces: `restoreFromTrash(root: string, id: string): Promise<void>`、`purgeTrashEntry(root: string, id: string): Promise<void>`（Task 6 が消費）

- [ ] **Step 1: 失敗するテストを書く**

`apps/server/src/features/trash/trash.service.test.ts` の先頭の import を変更し、`restoreFromTrash` / `purgeTrashEntry` を追加:

```ts
import {
  listTrash,
  moveToTrash,
  purgeTrashEntry,
  restoreFromTrash,
  TRASH_DIR_NAME,
} from "./trash.service";
```

続けて、末尾に追加:

```ts
describe("restoreFromTrash", () => {
  it("元の場所に戻す", async () => {
    await writeFile(path.join(root, "a.txt"), "hello");
    await moveToTrash(root, "a.txt");
    const [entry] = await listTrash(root);

    await restoreFromTrash(root, entry.id);

    expect(await readFile(path.join(root, "a.txt"), "utf8")).toBe("hello");
    expect(await listTrash(root)).toEqual([]);
  });

  it("元の親フォルダが削除済みでも自動再作成して復元できる", async () => {
    await mkdir(path.join(root, "sub"));
    await writeFile(path.join(root, "sub/a.txt"), "hello");
    await moveToTrash(root, "sub/a.txt");
    await rm(path.join(root, "sub"), { recursive: true });
    const [entry] = await listTrash(root);

    await restoreFromTrash(root, entry.id);

    expect(await readFile(path.join(root, "sub/a.txt"), "utf8")).toBe("hello");
  });

  it("存在しない id は NOT_FOUND", async () => {
    await expectAppError(restoreFromTrash(root, "missing-id"), "NOT_FOUND");
  });

  it("元の場所に同名の項目が既にあれば CONFLICT", async () => {
    await writeFile(path.join(root, "a.txt"), "hello");
    await moveToTrash(root, "a.txt");
    const [entry] = await listTrash(root);
    await writeFile(path.join(root, "a.txt"), "new file at same path");

    await expectAppError(restoreFromTrash(root, entry.id), "CONFLICT");
  });
});

describe("purgeTrashEntry", () => {
  it(".trash/<id>/ と <id>.json の両方を削除する", async () => {
    await writeFile(path.join(root, "a.txt"), "hello");
    await moveToTrash(root, "a.txt");
    const [entry] = await listTrash(root);

    await purgeTrashEntry(root, entry.id);

    const remaining = await readdir(path.join(root, TRASH_DIR_NAME));
    expect(remaining).toEqual([]);
  });

  it("存在しない id は NOT_FOUND", async () => {
    await expectAppError(purgeTrashEntry(root, "missing-id"), "NOT_FOUND");
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `npm run test -w @nas-fm/server -- trash.service`
Expected: FAIL（`restoreFromTrash` / `purgeTrashEntry` が未定義）

- [ ] **Step 3: import を更新し、実装を追加**

`apps/server/src/features/trash/trash.service.ts` の import 行を変更（`safeResolve` は既存 import のまま維持）:

```ts
import { AppError, fromFsError } from "../../lib/errors";
import { safeResolve } from "../../lib/safe-resolve";
```

ファイル末尾に追加:

```ts
export async function restoreFromTrash(root: string, id: string): Promise<void> {
  const dir = trashRoot(root);
  const meta = await readMeta(path.join(dir, `${id}.json`));
  if (!meta) {
    throw new AppError("NOT_FOUND", `trash entry not found: ${id}`);
  }
  const itemDir = path.join(dir, id);
  const itemNames = await fs.readdir(itemDir).catch(() => []);
  const name = itemNames[0];
  if (!name) {
    throw new AppError("NOT_FOUND", `trash entry not found: ${id}`);
  }

  const dest = safeResolve(root, meta.originalPath);
  const existing = await fs.lstat(dest).catch(() => null);
  if (existing) {
    throw new AppError("CONFLICT", `already exists: ${meta.originalPath}`);
  }
  await fs.mkdir(path.dirname(dest), { recursive: true });
  try {
    await fs.rename(path.join(itemDir, name), dest);
  } catch (err) {
    throw fromFsError(err, meta.originalPath);
  }
  await removeTrashFiles(root, id);
}

export async function purgeTrashEntry(root: string, id: string): Promise<void> {
  const meta = await readMeta(path.join(trashRoot(root), `${id}.json`));
  if (!meta) {
    throw new AppError("NOT_FOUND", `trash entry not found: ${id}`);
  }
  await removeTrashFiles(root, id);
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `npm run test -w @nas-fm/server -- trash.service`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add apps/server/src/features/trash/trash.service.ts apps/server/src/features/trash/trash.service.test.ts
git commit -m "$(cat <<'EOF'
feat: restoreFromTrashとpurgeTrashEntryを追加

EOF
)"
```

---

### Task 5: `trash.routes.ts` と `app.ts` への配線

**Files:**
- Create: `apps/server/src/features/trash/trash.routes.ts`
- Test: `apps/server/src/features/trash/trash.routes.test.ts`
- Modify: `apps/server/src/app.ts`

**Interfaces:**
- Consumes: `listTrash` / `restoreFromTrash` / `purgeTrashEntry`（Task 3, 4）
- Produces: `createTrashRoutes(root: string): Hono`（`app.ts` が消費）、エンドポイント `GET /api/trash` / `POST /api/trash/restore` / `DELETE /api/trash`

- [ ] **Step 1: 失敗するテストを書く**

`apps/server/src/features/trash/trash.routes.test.ts` を新規作成:

```ts
import { mkdtemp, rm, writeFile } from "node:fs/promises";
import { tmpdir } from "node:os";
import path from "node:path";
import { afterEach, beforeEach, describe, expect, it } from "vitest";
import type { ApiError, TrashListResponse } from "@nas-fm/shared";
import { createApp } from "../../app";
import type { AuthConfig } from "../../lib/auth-config";
import { hashPassword } from "../../lib/password";
import { issueToken } from "../auth/auth.service";
import { moveToTrash } from "./trash.service";

let root: string;
const authConfig: AuthConfig = { secret: "test-secret", passwordHash: hashPassword("pw") };
let authCookie: string;

beforeEach(async () => {
  root = await mkdtemp(path.join(tmpdir(), "nasfm-trash-routes-"));
  authCookie = `nasfm_token=${await issueToken(authConfig)}`;
});

afterEach(async () => {
  await rm(root, { recursive: true, force: true });
});

function withAuth(init: RequestInit = {}): RequestInit {
  return { ...init, headers: { ...(init.headers ?? {}), Cookie: authCookie } };
}

const jsonHeaders = { "content-type": "application/json" };

describe("GET /api/trash", () => {
  it("未認証は 401", async () => {
    const app = createApp(root, authConfig);
    const res = await app.request("/api/trash");
    expect(res.status).toBe(401);
  });

  it("移動済みの項目を返す", async () => {
    await writeFile(path.join(root, "a.txt"), "hello");
    await moveToTrash(root, "a.txt");
    const app = createApp(root, authConfig);
    const res = await app.request("/api/trash", withAuth());
    expect(res.status).toBe(200);
    const body = (await res.json()) as TrashListResponse;
    expect(body.entries).toHaveLength(1);
    expect(body.entries[0].name).toBe("a.txt");
  });
});

describe("POST /api/trash/restore", () => {
  it("復元できる", async () => {
    await writeFile(path.join(root, "a.txt"), "hello");
    await moveToTrash(root, "a.txt");
    const app = createApp(root, authConfig);
    const listRes = await app.request("/api/trash", withAuth());
    const { entries } = (await listRes.json()) as TrashListResponse;

    const res = await app.request(
      "/api/trash/restore",
      withAuth({ method: "POST", headers: jsonHeaders, body: JSON.stringify({ id: entries[0].id }) }),
    );
    expect(res.status).toBe(200);
  });

  it("id が無いボディは 400", async () => {
    const app = createApp(root, authConfig);
    const res = await app.request(
      "/api/trash/restore",
      withAuth({ method: "POST", headers: jsonHeaders, body: JSON.stringify({}) }),
    );
    expect(res.status).toBe(400);
  });

  it("存在しない id は 404", async () => {
    const app = createApp(root, authConfig);
    const res = await app.request(
      "/api/trash/restore",
      withAuth({
        method: "POST",
        headers: jsonHeaders,
        body: JSON.stringify({ id: "missing" }),
      }),
    );
    expect(res.status).toBe(404);
    const body = (await res.json()) as ApiError;
    expect(body.error.code).toBe("NOT_FOUND");
  });
});

describe("DELETE /api/trash", () => {
  it("完全削除できる", async () => {
    await writeFile(path.join(root, "a.txt"), "hello");
    await moveToTrash(root, "a.txt");
    const app = createApp(root, authConfig);
    const listRes = await app.request("/api/trash", withAuth());
    const { entries } = (await listRes.json()) as TrashListResponse;

    const res = await app.request(`/api/trash?id=${entries[0].id}`, withAuth({ method: "DELETE" }));
    expect(res.status).toBe(200);

    const afterRes = await app.request("/api/trash", withAuth());
    const after = (await afterRes.json()) as TrashListResponse;
    expect(after.entries).toEqual([]);
  });

  it("id 未指定は 400", async () => {
    const app = createApp(root, authConfig);
    const res = await app.request("/api/trash", withAuth({ method: "DELETE" }));
    expect(res.status).toBe(400);
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `npm run test -w @nas-fm/server -- trash.routes`
Expected: FAIL（`/api/trash` 系ルートが未定義のため 404、または `trash.routes` モジュールが存在しない）

- [ ] **Step 3: ルートを実装**

`apps/server/src/features/trash/trash.routes.ts` を新規作成:

```ts
import { Hono } from "hono";
import type { OkResponse, TrashListResponse, TrashRestoreRequest } from "@nas-fm/shared";
import { AppError } from "../../lib/errors";
import { listTrash, purgeTrashEntry, restoreFromTrash } from "./trash.service";

function parseRestoreBody(value: unknown): TrashRestoreRequest {
  if (
    typeof value !== "object" ||
    value === null ||
    typeof (value as { id?: unknown }).id !== "string" ||
    (value as { id: string }).id === ""
  ) {
    throw new AppError("INVALID_REQUEST", "body must be { id: string }");
  }
  return { id: (value as { id: string }).id };
}

export function createTrashRoutes(root: string): Hono {
  const app = new Hono();

  app.get("/trash", async (c) => {
    const entries = await listTrash(root);
    const res: TrashListResponse = { entries };
    return c.json(res);
  });

  app.post("/trash/restore", async (c) => {
    let body: unknown;
    try {
      body = await c.req.json();
    } catch {
      throw new AppError("INVALID_REQUEST", "invalid JSON body");
    }
    const { id } = parseRestoreBody(body);
    await restoreFromTrash(root, id);
    const res: OkResponse = { ok: true };
    return c.json(res);
  });

  app.delete("/trash", async (c) => {
    const id = c.req.query("id");
    if (!id) {
      throw new AppError("INVALID_REQUEST", "id is required");
    }
    await purgeTrashEntry(root, id);
    const res: OkResponse = { ok: true };
    return c.json(res);
  });

  return app;
}
```

- [ ] **Step 4: `app.ts` に配線**

`apps/server/src/app.ts` の import ブロックに追加:

```ts
import { createTrashRoutes } from "./features/trash/trash.routes";
```

`app.route("/api", createFilesRoutes(root));` の直後に追加:

```ts
  app.route("/api", createTrashRoutes(root));
```

- [ ] **Step 5: テストが通ることを確認**

Run: `npm run test -w @nas-fm/server -- trash.routes`
Expected: PASS

- [ ] **Step 6: サーバ全体のテスト・型チェックを確認**

Run: `npm run test -w @nas-fm/server && npm run typecheck -w @nas-fm/server`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add apps/server/src/features/trash/trash.routes.ts apps/server/src/features/trash/trash.routes.test.ts apps/server/src/app.ts
git commit -m "$(cat <<'EOF'
feat: ゴミ箱のAPIエンドポイントを追加

EOF
)"
```

---

### Task 6: `files` feature を `.trash` 対応に変更（一覧・zip除外、delete のソフトデリート化）

**Files:**
- Modify: `apps/server/src/features/files/files.service.ts`
- Modify: `apps/server/src/features/files/files.service.test.ts`
- Modify: `apps/server/src/features/files/files.routes.ts`
- Modify: `apps/server/src/features/files/files.routes.test.ts`

**Interfaces:**
- Consumes: `TRASH_DIR_NAME`（Task 2）、`moveToTrash`（Task 2）

- [ ] **Step 1: `files.service.test.ts` から `removePath` のテストを削除**

`apps/server/src/features/files/files.service.test.ts` の78〜99行目（`describe("removePath", ...)` ブロック全体）を削除する。また1行目の import から `removePath` を除く:

```ts
import {
  createFolderZipStream,
  listDir,
  makeDir,
  renamePath,
  resolveDownloadEntry,
  statForDownload,
  uploadFile,
} from "./files.service";
```

- [ ] **Step 2: `listDir` / `walkAndAppend` の `.trash` 除外テストを追加**

`files.service.test.ts` の `describe("listDir", ...)` ブロック内（既存テストの後）に追加:

```ts
  it(".trash ディレクトリは一覧に含まれない", async () => {
    await writeFile(path.join(root, "a.txt"), "hello");
    await mkdir(path.join(root, ".trash"));
    await writeFile(path.join(root, ".trash", "leftover.txt"), "x");
    const entries = await listDir(root, "");
    expect(entries.map((e) => e.name)).toEqual(["a.txt"]);
  });
```

既存の `describe("createFolderZipStream", ...)` ブロック内（`zipToEntries` ヘルパーが既に定義済み）の最後のテストの後に追加:

```ts
  it(".trash ディレクトリは zip に含めない", async () => {
    const dir = path.join(root, "folder");
    await mkdir(dir);
    await writeFile(path.join(dir, "a.txt"), "hello");
    await mkdir(path.join(dir, ".trash"));
    await writeFile(path.join(dir, ".trash", "leftover.txt"), "x");

    const archive = createFolderZipStream(dir);
    const zipPath = path.join(root, "out-trash.zip");
    const names = await zipToEntries(archive, zipPath);
    expect(names).toEqual(["a.txt"]);
  });
```

- [ ] **Step 3: ルートレベルの `.trash` 除外テストを追加**

`apps/server/src/features/files/files.routes.test.ts` の `describe("GET /api/list", ...)` ブロック内（既存テストの後）に追加:

```ts
  it("削除でゴミ箱へ移動した項目は一覧に出ない（.trash 自体も出ない）", async () => {
    await writeFile(path.join(root, "a.txt"), "x");
    const app = createApp(root, authConfig);
    await app.request("/api/delete?path=a.txt", withAuth({ method: "DELETE" }));
    const res = await app.request("/api/list?path=", withAuth());
    const body = (await res.json()) as ListResponse;
    expect(body.entries).toEqual([]);
  });
```

`describe("GET /api/download", ...)` ブロック内（既存テストの後）に追加:

```ts
  it("ルート直下の zip ダウンロードに .trash 配下は含まれない", async () => {
    await writeFile(path.join(root, "a.txt"), "hello");
    const app = createApp(root, authConfig);
    await app.request("/api/delete?path=a.txt", withAuth({ method: "DELETE" }));
    await writeFile(path.join(root, "b.txt"), "still here");

    const res = await app.request("/api/download?path=", withAuth());
    expect(res.status).toBe(200);
    const zipPath = path.join(root, "root-download.zip");
    await pipeline(Readable.fromWeb(res.body as never), createWriteStream(zipPath));
    const zip = new AdmZip(zipPath);
    expect(zip.getEntries().map((e) => e.entryName)).toEqual(["b.txt"]);
  });
```

- [ ] **Step 4: テストが失敗することを確認**

Run: `npm run test -w @nas-fm/server -- files.service files.routes`
Expected: FAIL（`.trash` がまだ除外されていない。また `removePath` の import 削除により、他のテストがコンパイルエラーにならないことも確認する）

- [ ] **Step 5: `files.service.ts` を変更**

先頭の import に `TRASH_DIR_NAME` を追加:

```ts
import { createWriteStream } from "node:fs";
import fs from "node:fs/promises";
import path from "node:path";
import type { Readable } from "node:stream";
import { pipeline } from "node:stream/promises";
import { ZipArchive, type Archiver } from "archiver";
import type { FileEntry } from "@nas-fm/shared";
import { AppError, fromFsError } from "../../lib/errors";
import { safeResolve } from "../../lib/safe-resolve";
import { TRASH_DIR_NAME } from "../trash/trash.service";
```

`listDir` を変更（`.trash` を除外する1行を追加）:

```ts
export async function listDir(root: string, relPath: string): Promise<FileEntry[]> {
  const abs = safeResolve(root, relPath);
  let names: string[];
  try {
    names = await fs.readdir(abs);
  } catch (err) {
    throw fromFsError(err, relPath);
  }
  const entries: FileEntry[] = [];
  for (const name of names) {
    if (name === TRASH_DIR_NAME) continue;
    const st = await fs.stat(path.join(abs, name)).catch(() => null);
    if (!st) continue; // 列挙後に消えたエントリはスキップ
    const isDir = st.isDirectory();
    entries.push({
      name,
      size: isDir ? 0 : st.size,
      mtime: Math.trunc(st.mtimeMs),
      type: isDir ? "dir" : "file",
    });
  }
  return entries;
}
```

`removePath` 関数（34〜44行目）を削除する。

`walkAndAppend` を変更（`.trash` を除外する1行を追加）:

```ts
async function walkAndAppend(archive: Archiver, absDir: string, zipPrefix: string): Promise<void> {
  const entries = await fs.readdir(absDir, { withFileTypes: true });
  for (const entry of entries) {
    if (entry.isSymbolicLink()) continue;
    if (entry.name === TRASH_DIR_NAME) continue;
    const absPath = path.join(absDir, entry.name);
    const zipPath = zipPrefix ? `${zipPrefix}/${entry.name}` : entry.name;
    if (entry.isDirectory()) {
      await walkAndAppend(archive, absPath, zipPath);
    } else if (entry.isFile()) {
      archive.file(absPath, { name: zipPath });
    }
  }
}
```

- [ ] **Step 6: `files.routes.ts` を変更（`DELETE /delete` を `moveToTrash` に差し替え）**

ファイル冒頭の import ブロック全体を以下に置き換える（`files.service` の import から `removePath` を削除し、`../trash/trash.service` の import を末尾に追加）:

```ts
import { createReadStream } from "node:fs";
import { Readable } from "node:stream";
import type { ReadableStream as NodeWebReadableStream } from "node:stream/web";
import { Hono } from "hono";
import type { ListResponse, OkResponse } from "@nas-fm/shared";
import { classifyPreview } from "@nas-fm/shared";
import { AppError } from "../../lib/errors";
import { previewContentType } from "../../lib/preview-mime";
import { parseRange } from "../../lib/range";
import {
  createFolderZipStream,
  listDir,
  makeDir,
  renamePath,
  resolveDownloadEntry,
  statForDownload,
  uploadFile,
} from "./files.service";
import { optionalPath, parseMkdirBody, parseRenameBody, requirePath } from "./files.schema";
import { moveToTrash } from "../trash/trash.service";
```

`app.delete("/delete", ...)` ハンドラを変更:

```ts
  app.delete("/delete", async (c) => {
    const rel = requirePath(c.req.query("path"));
    await moveToTrash(root, rel);
    const res: OkResponse = { ok: true };
    return c.json(res);
  });
```

- [ ] **Step 7: テストが通ることを確認**

Run: `npm run test -w @nas-fm/server -- files.service files.routes`
Expected: PASS（既存の `DELETE /api/delete` テスト「ファイルを削除する」「存在しないパスは404」「rootの削除は400」は無変更のまま通る）

- [ ] **Step 8: サーバ全体のテスト・型チェックを確認**

Run: `npm run test -w @nas-fm/server && npm run typecheck -w @nas-fm/server`
Expected: PASS

- [ ] **Step 9: Commit**

```bash
git add apps/server/src/features/files/files.service.ts apps/server/src/features/files/files.service.test.ts apps/server/src/features/files/files.routes.ts apps/server/src/features/files/files.routes.test.ts
git commit -m "$(cat <<'EOF'
feat: 削除をゴミ箱への移動に変更し.trashを一覧・zipから除外する

EOF
)"
```

---

### Task 7: フロント `api.ts` にゴミ箱系メソッドを追加

**Files:**
- Modify: `apps/web/src/lib/api.ts`

**Interfaces:**
- Consumes: `TrashListResponse`（Task 1、`@nas-fm/shared`）
- Produces: `api.listTrash(): Promise<TrashListResponse>`、`api.restoreFromTrash(id: string): Promise<void>`、`api.purgeTrashEntry(id: string): Promise<void>`（Task 8, 9 が消費）

- [ ] **Step 1: import に型を追加**

`apps/web/src/lib/api.ts` の1行目を変更:

```ts
import type { AuthStatus, DiskUsageResponse, ListResponse, TrashListResponse } from "@nas-fm/shared";
```

- [ ] **Step 2: メソッドを追加**

`api.remove` メソッド定義の直後に追加:

```ts
  async listTrash(): Promise<TrashListResponse> {
    const res = await request("/api/trash");
    return (await res.json()) as TrashListResponse;
  },

  async restoreFromTrash(id: string): Promise<void> {
    await request("/api/trash/restore", {
      method: "POST",
      headers: JSON_HEADERS,
      body: JSON.stringify({ id }),
    });
  },

  async purgeTrashEntry(id: string): Promise<void> {
    await request(`/api/trash?id=${encodeURIComponent(id)}`, { method: "DELETE" });
  },
```

- [ ] **Step 3: 型チェックが通ることを確認**

Run: `npm run typecheck -w @nas-fm/web`
Expected: エラーなし

- [ ] **Step 4: Commit**

```bash
git add apps/web/src/lib/api.ts
git commit -m "$(cat <<'EOF'
feat: api.listTrash等のゴミ箱APIクライアントを追加

EOF
)"
```

---

### Task 8: フロント `useTrash` フック

**Files:**
- Create: `apps/web/src/features/trash/hooks/useTrash.ts`
- Test: `apps/web/src/features/trash/hooks/useTrash.test.tsx`

**Interfaces:**
- Consumes: `api.listTrash()`（Task 7）
- Produces: `useTrash()`（`UseQueryResult<TrashListResponse>`、queryKey `["trash"]`。Task 11 が消費）

- [ ] **Step 1: 失敗するテストを書く**

`apps/web/src/features/trash/hooks/useTrash.test.tsx` を新規作成:

```tsx
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { renderHook, waitFor } from "@testing-library/react";
import type { ReactNode } from "react";
import { afterEach, describe, expect, it, vi } from "vitest";
import { api } from "@/lib/api";
import { useTrash } from "./useTrash";

function wrapper({ children }: { children: ReactNode }) {
  const client = new QueryClient({ defaultOptions: { queries: { retry: false } } });
  return <QueryClientProvider client={client}>{children}</QueryClientProvider>;
}

afterEach(() => vi.restoreAllMocks());

describe("useTrash", () => {
  it("ゴミ箱の一覧を取得する", async () => {
    vi.spyOn(api, "listTrash").mockResolvedValue({
      entries: [
        { id: "1", name: "a.txt", originalPath: "a.txt", type: "file", size: 5, deletedAt: 1 },
      ],
    });
    const { result } = renderHook(() => useTrash(), { wrapper });
    await waitFor(() => expect(result.current.data?.entries).toHaveLength(1));
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `npm run test -w @nas-fm/web -- useTrash`
Expected: FAIL（`useTrash` モジュールが存在しない）

- [ ] **Step 3: 最小実装を書く**

`apps/web/src/features/trash/hooks/useTrash.ts` を新規作成:

```ts
import { useQuery } from "@tanstack/react-query";
import { api } from "@/lib/api";

export function useTrash() {
  return useQuery({ queryKey: ["trash"], queryFn: () => api.listTrash() });
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `npm run test -w @nas-fm/web -- useTrash`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/features/trash/hooks/useTrash.ts apps/web/src/features/trash/hooks/useTrash.test.tsx
git commit -m "$(cat <<'EOF'
feat: useTrashフックを追加

EOF
)"
```

---

### Task 9: フロント `useTrashMutations` フック

**Files:**
- Create: `apps/web/src/features/trash/hooks/useTrashMutations.ts`
- Test: `apps/web/src/features/trash/hooks/useTrashMutations.test.tsx`

**Interfaces:**
- Consumes: `api.restoreFromTrash(id)` / `api.purgeTrashEntry(id)`（Task 7）
- Produces: `useTrashMutations(): { restore: UseMutationResult<...>; purge: UseMutationResult<...> }`（Task 10 が消費。`restore.mutate(id: string)` / `purge.mutate(id: string)`）

- [ ] **Step 1: 失敗するテストを書く**

`apps/web/src/features/trash/hooks/useTrashMutations.test.tsx` を新規作成:

```tsx
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { renderHook, waitFor } from "@testing-library/react";
import type { ReactNode } from "react";
import { afterEach, describe, expect, it, vi } from "vitest";
import { toast } from "sonner";
import { api } from "@/lib/api";
import { useTrashMutations } from "./useTrashMutations";

function wrapperWithClient(client: QueryClient) {
  return function Wrapper({ children }: { children: ReactNode }) {
    return <QueryClientProvider client={client}>{children}</QueryClientProvider>;
  };
}

afterEach(() => vi.restoreAllMocks());

describe("useTrashMutations", () => {
  it("restore 成功時に trash と list を再取得し成功トーストを出す", async () => {
    vi.spyOn(api, "restoreFromTrash").mockResolvedValue();
    const success = vi.spyOn(toast, "success").mockReturnValue("" as never);
    const client = new QueryClient({
      defaultOptions: { queries: { retry: false }, mutations: { retry: false } },
    });
    const invalidateQueries = vi.spyOn(client, "invalidateQueries");
    const { result } = renderHook(() => useTrashMutations(), { wrapper: wrapperWithClient(client) });

    result.current.restore.mutate("id-1");

    await waitFor(() => expect(invalidateQueries).toHaveBeenCalledWith({ queryKey: ["trash"] }));
    expect(invalidateQueries).toHaveBeenCalledWith({ queryKey: ["list"] });
    expect(success).toHaveBeenCalled();
  });

  it("purge 成功時に trash と disk-usage を再取得し成功トーストを出す", async () => {
    vi.spyOn(api, "purgeTrashEntry").mockResolvedValue();
    const success = vi.spyOn(toast, "success").mockReturnValue("" as never);
    const client = new QueryClient({
      defaultOptions: { queries: { retry: false }, mutations: { retry: false } },
    });
    const invalidateQueries = vi.spyOn(client, "invalidateQueries");
    const { result } = renderHook(() => useTrashMutations(), { wrapper: wrapperWithClient(client) });

    result.current.purge.mutate("id-1");

    await waitFor(() => expect(invalidateQueries).toHaveBeenCalledWith({ queryKey: ["trash"] }));
    expect(invalidateQueries).toHaveBeenCalledWith({ queryKey: ["disk-usage"] });
    expect(success).toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `npm run test -w @nas-fm/web -- useTrashMutations`
Expected: FAIL（`useTrashMutations` モジュールが存在しない）

- [ ] **Step 3: 最小実装を書く**

`apps/web/src/features/trash/hooks/useTrashMutations.ts` を新規作成:

```ts
import { useMutation, useQueryClient } from "@tanstack/react-query";
import { toast } from "sonner";
import { ApiRequestError, api } from "@/lib/api";
import { errorMessage } from "@/lib/error-messages";

function toastError(err: unknown): void {
  const code = err instanceof ApiRequestError ? err.code : "INTERNAL";
  toast.error(errorMessage(code));
}

export function useTrashMutations() {
  const qc = useQueryClient();
  const invalidateTrash = () => qc.invalidateQueries({ queryKey: ["trash"] });

  const restore = useMutation({
    mutationFn: (id: string) => api.restoreFromTrash(id),
    onSuccess: () => {
      invalidateTrash();
      qc.invalidateQueries({ queryKey: ["list"] });
      toast.success("復元しました");
    },
    onError: toastError,
  });

  const purge = useMutation({
    mutationFn: (id: string) => api.purgeTrashEntry(id),
    onSuccess: () => {
      invalidateTrash();
      qc.invalidateQueries({ queryKey: ["disk-usage"] });
      toast.success("完全に削除しました");
    },
    onError: toastError,
  });

  return { restore, purge };
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `npm run test -w @nas-fm/web -- useTrashMutations`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/features/trash/hooks/useTrashMutations.ts apps/web/src/features/trash/hooks/useTrashMutations.test.tsx
git commit -m "$(cat <<'EOF'
feat: useTrashMutationsフックを追加

EOF
)"
```

---

### Task 10: フロント `TrashDialog` コンポーネント

**Files:**
- Create: `apps/web/src/features/trash/components/TrashDialog.tsx`
- Test: `apps/web/src/features/trash/components/TrashDialog.test.tsx`

**Interfaces:**
- Consumes: `useTrash()`（Task 8）、`useTrashMutations()`（Task 9）
- Produces: `TrashDialog`（React コンポーネント、props `{ open: boolean; onOpenChange: (v: boolean) => void }`。Task 11 が消費）

- [ ] **Step 1: 失敗するテストを書く**

`apps/web/src/features/trash/components/TrashDialog.test.tsx` を新規作成:

```tsx
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import type { ReactNode } from "react";
import { afterEach, describe, expect, it, vi } from "vitest";
import { api } from "@/lib/api";
import { TrashDialog } from "./TrashDialog";

function renderWithClient(ui: ReactNode) {
  const client = new QueryClient({ defaultOptions: { queries: { retry: false } } });
  return render(<QueryClientProvider client={client}>{ui}</QueryClientProvider>);
}

afterEach(() => vi.restoreAllMocks());

describe("TrashDialog", () => {
  it("空のときは「ゴミ箱は空です」と表示する", async () => {
    vi.spyOn(api, "listTrash").mockResolvedValue({ entries: [] });
    renderWithClient(<TrashDialog open onOpenChange={() => {}} />);
    expect(await screen.findByText("ゴミ箱は空です")).toBeInTheDocument();
  });

  it("項目の名前・元の場所を一覧表示する", async () => {
    vi.spyOn(api, "listTrash").mockResolvedValue({
      entries: [
        {
          id: "1",
          name: "a.txt",
          originalPath: "docs/a.txt",
          type: "file",
          size: 5,
          deletedAt: Date.now(),
        },
      ],
    });
    renderWithClient(<TrashDialog open onOpenChange={() => {}} />);
    expect(await screen.findByText("a.txt")).toBeInTheDocument();
    expect(screen.getByText("docs/a.txt")).toBeInTheDocument();
  });

  it("「復元」クリックで restore が呼ばれる", async () => {
    vi.spyOn(api, "listTrash").mockResolvedValue({
      entries: [
        { id: "1", name: "a.txt", originalPath: "a.txt", type: "file", size: 5, deletedAt: 1 },
      ],
    });
    const restoreSpy = vi.spyOn(api, "restoreFromTrash").mockResolvedValue();
    renderWithClient(<TrashDialog open onOpenChange={() => {}} />);
    await userEvent.click(await screen.findByRole("button", { name: "復元" }));
    expect(restoreSpy).toHaveBeenCalledWith("1");
  });

  it("「完全に削除」は確認ダイアログを経てから purge を呼ぶ", async () => {
    vi.spyOn(api, "listTrash").mockResolvedValue({
      entries: [
        { id: "1", name: "a.txt", originalPath: "a.txt", type: "file", size: 5, deletedAt: 1 },
      ],
    });
    const purgeSpy = vi.spyOn(api, "purgeTrashEntry").mockResolvedValue();
    renderWithClient(<TrashDialog open onOpenChange={() => {}} />);
    await userEvent.click(await screen.findByRole("button", { name: "完全に削除" }));
    expect(purgeSpy).not.toHaveBeenCalled();
    await userEvent.click(await screen.findByRole("button", { name: "完全に削除する" }));
    expect(purgeSpy).toHaveBeenCalledWith("1");
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `npm run test -w @nas-fm/web -- TrashDialog`
Expected: FAIL（`TrashDialog` モジュールが存在しない）

- [ ] **Step 3: 最小実装を書く**

`apps/web/src/features/trash/components/TrashDialog.tsx` を新規作成:

```tsx
import { useState } from "react";
import type { TrashEntry } from "@nas-fm/shared";
import {
  AlertDialog,
  AlertDialogAction,
  AlertDialogCancel,
  AlertDialogContent,
  AlertDialogDescription,
  AlertDialogFooter,
  AlertDialogHeader,
  AlertDialogTitle,
} from "@/components/ui/alert-dialog";
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import {
  Table,
  TableBody,
  TableCell,
  TableHead,
  TableHeader,
  TableRow,
} from "@/components/ui/table";
import { useTrash } from "../hooks/useTrash";
import { useTrashMutations } from "../hooks/useTrashMutations";

function formatSize(entry: TrashEntry): string {
  if (entry.type === "dir") return "—";
  if (entry.size < 1024) return `${entry.size} B`;
  if (entry.size < 1024 * 1024) return `${(entry.size / 1024).toFixed(1)} KB`;
  if (entry.size < 1024 * 1024 * 1024) return `${(entry.size / 1024 / 1024).toFixed(1)} MB`;
  return `${(entry.size / 1024 / 1024 / 1024).toFixed(1)} GB`;
}

export function TrashDialog({
  open,
  onOpenChange,
}: {
  open: boolean;
  onOpenChange: (v: boolean) => void;
}) {
  const { data } = useTrash();
  const { restore, purge } = useTrashMutations();
  const [purgeTarget, setPurgeTarget] = useState<TrashEntry | null>(null);
  const entries = data?.entries ?? [];

  return (
    <>
      <Dialog open={open} onOpenChange={onOpenChange}>
        <DialogContent className="max-w-2xl">
          <DialogHeader>
            <DialogTitle>ゴミ箱</DialogTitle>
          </DialogHeader>
          {entries.length === 0 && <p className="text-sm text-muted-foreground">ゴミ箱は空です</p>}
          {entries.length > 0 && (
            <Table>
              <TableHeader>
                <TableRow>
                  <TableHead>名前</TableHead>
                  <TableHead>元の場所</TableHead>
                  <TableHead>サイズ</TableHead>
                  <TableHead>削除日時</TableHead>
                  <TableHead className="w-48" />
                </TableRow>
              </TableHeader>
              <TableBody>
                {entries.map((entry) => (
                  <TableRow key={entry.id}>
                    <TableCell>{entry.name}</TableCell>
                    <TableCell>{entry.originalPath}</TableCell>
                    <TableCell>{formatSize(entry)}</TableCell>
                    <TableCell>{new Date(entry.deletedAt).toLocaleString("ja-JP")}</TableCell>
                    <TableCell className="flex gap-2">
                      <Button variant="outline" size="sm" onClick={() => restore.mutate(entry.id)}>
                        復元
                      </Button>
                      <Button variant="destructive" size="sm" onClick={() => setPurgeTarget(entry)}>
                        完全に削除
                      </Button>
                    </TableCell>
                  </TableRow>
                ))}
              </TableBody>
            </Table>
          )}
        </DialogContent>
      </Dialog>
      <AlertDialog open={purgeTarget !== null} onOpenChange={(v) => !v && setPurgeTarget(null)}>
        <AlertDialogContent>
          <AlertDialogHeader>
            <AlertDialogTitle>完全に削除しますか</AlertDialogTitle>
            <AlertDialogDescription>
              「{purgeTarget?.name}」を完全に削除します。この操作は取り消せません。
            </AlertDialogDescription>
          </AlertDialogHeader>
          <AlertDialogFooter>
            <AlertDialogCancel>キャンセル</AlertDialogCancel>
            <AlertDialogAction
              onClick={() => {
                if (purgeTarget) purge.mutate(purgeTarget.id);
                setPurgeTarget(null);
              }}
            >
              完全に削除する
            </AlertDialogAction>
          </AlertDialogFooter>
        </AlertDialogContent>
      </AlertDialog>
    </>
  );
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `npm run test -w @nas-fm/web -- TrashDialog`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add apps/web/src/features/trash/components/TrashDialog.tsx apps/web/src/features/trash/components/TrashDialog.test.tsx
git commit -m "$(cat <<'EOF'
feat: TrashDialogコンポーネントを追加

EOF
)"
```

---

### Task 11: フロント `TrashButton` コンポーネントと feature の公開境界

**Files:**
- Create: `apps/web/src/features/trash/components/TrashButton.tsx`
- Test: `apps/web/src/features/trash/components/TrashButton.test.tsx`
- Create: `apps/web/src/features/trash/index.ts`

**Interfaces:**
- Consumes: `TrashDialog`（Task 10）
- Produces: `TrashButton`（React コンポーネント。Task 12 が `@/features/trash` 経由で消費）

- [ ] **Step 1: 失敗するテストを書く**

`apps/web/src/features/trash/components/TrashButton.test.tsx` を新規作成:

```tsx
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import type { ReactNode } from "react";
import { afterEach, describe, expect, it, vi } from "vitest";
import { api } from "@/lib/api";
import { TrashButton } from "./TrashButton";

function renderWithClient(ui: ReactNode) {
  const client = new QueryClient({ defaultOptions: { queries: { retry: false } } });
  return render(<QueryClientProvider client={client}>{ui}</QueryClientProvider>);
}

afterEach(() => vi.restoreAllMocks());

describe("TrashButton", () => {
  it("クリックするとゴミ箱ダイアログを開く", async () => {
    vi.spyOn(api, "listTrash").mockResolvedValue({ entries: [] });
    renderWithClient(<TrashButton />);
    expect(screen.queryByText("ゴミ箱は空です")).not.toBeInTheDocument();
    await userEvent.click(screen.getByRole("button", { name: "ゴミ箱" }));
    expect(await screen.findByText("ゴミ箱は空です")).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `npm run test -w @nas-fm/web -- TrashButton`
Expected: FAIL（`TrashButton` モジュールが存在しない）

- [ ] **Step 3: 最小実装を書く**

`apps/web/src/features/trash/components/TrashButton.tsx` を新規作成:

```tsx
import { useState } from "react";
import { Trash2 } from "lucide-react";
import { Button } from "@/components/ui/button";
import { TrashDialog } from "./TrashDialog";

export function TrashButton() {
  const [open, setOpen] = useState(false);
  return (
    <>
      <Button variant="ghost" size="icon-sm" aria-label="ゴミ箱" onClick={() => setOpen(true)}>
        <Trash2 size={16} />
      </Button>
      <TrashDialog open={open} onOpenChange={setOpen} />
    </>
  );
}
```

- [ ] **Step 4: `index.ts` を新規作成**

`apps/web/src/features/trash/index.ts` を新規作成:

```ts
export { TrashButton } from "./components/TrashButton";
```

- [ ] **Step 5: テストが通ることを確認**

Run: `npm run test -w @nas-fm/web -- TrashButton`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add apps/web/src/features/trash/components/TrashButton.tsx apps/web/src/features/trash/components/TrashButton.test.tsx apps/web/src/features/trash/index.ts
git commit -m "$(cat <<'EOF'
feat: TrashButtonコンポーネントを追加

EOF
)"
```

---

### Task 12: `App.tsx` の `Header` に配線

**Files:**
- Modify: `apps/web/src/app/App.tsx`
- Modify: `apps/web/src/app/App.test.tsx`

**Interfaces:**
- Consumes: `TrashButton`（Task 11、`@/features/trash` 経由）

- [ ] **Step 1: 失敗するテストを書く**

`apps/web/src/app/App.test.tsx` の3つ目のテスト（Feature 1 の作業で `api.diskUsage` のモックが既に追加されている前提）に `api.listTrash` のモックと表示アサーションを追加:

```tsx
  it("認証済みでは一覧（ログアウト）とディスク使用量とゴミ箱ボタンを表示する", async () => {
    vi.spyOn(api, "me").mockResolvedValue({ authenticated: true });
    vi.spyOn(api, "list").mockResolvedValue({ path: "", entries: [] });
    vi.spyOn(api, "diskUsage").mockResolvedValue({
      total: 100 * 1024 ** 3,
      used: 50 * 1024 ** 3,
      free: 50 * 1024 ** 3,
    });
    vi.spyOn(api, "listTrash").mockResolvedValue({ entries: [] });
    render(<App />);
    await waitFor(() =>
      expect(screen.getByRole("button", { name: "ログアウト" })).toBeInTheDocument(),
    );
    expect(screen.getByText("50.0GB / 100.0GB")).toBeInTheDocument();
    expect(screen.getByRole("button", { name: "ゴミ箱" })).toBeInTheDocument();
  });
```

（`api.diskUsage` のモックが無い場合は、Feature 1 のプランで追加された箇所と統合してこの内容にする。）

- [ ] **Step 2: テストが失敗することを確認**

Run: `npm run test -w @nas-fm/web -- App.test`
Expected: FAIL（`TrashButton` が未配線のため「ゴミ箱」ボタンが見つからない）

- [ ] **Step 3: `App.tsx` に配線**

`apps/web/src/app/App.tsx` を全体置き換え:

```tsx
import { AuthGate, LogoutButton, useAuth } from "@/features/auth";
import { DiskUsageBadge } from "@/features/disk-usage";
import { FileBrowser } from "@/features/file-list";
import { TrashButton } from "@/features/trash";
import { Providers } from "./providers";

function Header() {
  const { data } = useAuth();
  return (
    <header className="flex items-center justify-between border-b px-6 py-4">
      <h1 className="text-xl font-semibold">NAS-FileManager</h1>
      <div className="flex items-center gap-4">
        {data?.authenticated && <DiskUsageBadge />}
        {data?.authenticated && <TrashButton />}
        {data?.authenticated && <LogoutButton />}
      </div>
    </header>
  );
}

export function App() {
  return (
    <Providers>
      <div className="min-h-screen bg-background text-foreground">
        <Header />
        <main className="p-6">
          <AuthGate>
            <FileBrowser />
          </AuthGate>
        </main>
      </div>
    </Providers>
  );
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `npm run test -w @nas-fm/web -- App.test`
Expected: PASS

- [ ] **Step 5: web 全体のテスト・型チェックを確認**

Run: `npm run test -w @nas-fm/web && npm run typecheck -w @nas-fm/web`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add apps/web/src/app/App.tsx apps/web/src/app/App.test.tsx
git commit -m "$(cat <<'EOF'
feat: ヘッダーにゴミ箱ボタンを表示する

EOF
)"
```

---

### Task 13: `DeleteDialog` の文言を「取り消し可能」に更新

**Files:**
- Modify: `apps/web/src/features/file-list/dialogs/DeleteDialog.tsx`
- Modify: `apps/web/src/features/file-list/dialogs/DeleteDialog.test.tsx`

- [ ] **Step 1: 失敗するテストを書く**

`apps/web/src/features/file-list/dialogs/DeleteDialog.test.tsx` の既存テストに以下のアサーションを追加（`expect(screen.getByText(/a\.txt/)).toBeInTheDocument();` の直後）:

```tsx
    expect(screen.getByText(/ゴミ箱に移動します/)).toBeInTheDocument();
    expect(screen.queryByText(/この操作は取り消せません/)).not.toBeInTheDocument();
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `npm run test -w @nas-fm/web -- DeleteDialog`
Expected: FAIL（文言がまだ「削除します」「取り消せません」のまま）

- [ ] **Step 3: `DeleteDialog.tsx` の文言を変更**

`apps/web/src/features/file-list/dialogs/DeleteDialog.tsx` の `AlertDialogDescription` を変更:

```tsx
          <AlertDialogDescription>
            「{targetName}
            」をゴミ箱に移動します。フォルダの場合は中身ごと移動されます。ゴミ箱の項目は30日後に自動的に完全削除されます。
          </AlertDialogDescription>
```

- [ ] **Step 4: テストが通ることを確認**

Run: `npm run test -w @nas-fm/web -- DeleteDialog`
Expected: PASS

- [ ] **Step 5: web 全体のテスト・lint・型チェックを確認**

Run: `npm run test -w @nas-fm/web && npm run lint && npm run typecheck`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add apps/web/src/features/file-list/dialogs/DeleteDialog.tsx apps/web/src/features/file-list/dialogs/DeleteDialog.test.tsx
git commit -m "$(cat <<'EOF'
feat: 削除確認ダイアログの文言をゴミ箱移動に合わせて更新する

EOF
)"
```

---

## 完了後の確認

- `npm run dev` でログイン後、ファイルを削除 → ヘッダーの「ゴミ箱」ボタンを開いて削除した項目が表示されることを確認する
- 「復元」を押すと元のフォルダに戻ることを確認する
- 「完全に削除」→確認→実行で消えること、ディスク使用量表示（Feature 1 実装済みの場合）が更新されることを確認する
- フォルダごと削除した場合も同様に復元できることを確認する
- ルート直下を zip ダウンロードしたとき、zip の中に `.trash` が含まれないことを確認する
