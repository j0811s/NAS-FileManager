# /opt/nas-fm/nas-fm.env としてコピーし、実際の値に置き換えること。
# このファイルは Git 管理外にし、chmod 600 で保護する。
#
# AUTH_SECRET はランダムな長い文字列。
# AUTH_PASSWORD_HASH は `npx tsx apps/server/scripts/hash-password.ts <password>` の出力を使う。
AUTH_SECRET=<ランダムな長い文字列に置き換える>
AUTH_PASSWORD_HASH=<hash-password.ts で生成した値に置き換える>
